# Prior-claim register — new-user surface (auth / session / activation / registro / invites / claim / tenant)

Gathered 2026-09-02. Pure extraction, no new judgment. Every row is copied or tight-quoted from an
existing doc/memory, filtered to: auth, session, cookies, `proxy.ts`, activation, `/registro`,
invites, `send-email` hook, Resend, claim RPCs, `clientes`/`gym_membership` RLS, tenant/host
resolution, member removal, retries/idempotency. Analysts: attribute to the row id + cite, do not
re-derive without checking HEAD first. Catalog rows (`F-NN`) below carry `status: unverified` in
their source file as of this gather — that file is the not-yet-validated Session-V work list, not a
confirmed finding; treat accordingly.

Columns: `id | source (doc + row/line) | date | claim | stated basis | cite`.

---

## 1. 2026-09-01 breaking-points pack

### 1a. Top-8 ranked entries (filtered to this surface: #1, #4, #5, #8 of 8; #2/#3/#6/#7 are admin-desk/cron/roster, off-surface)

| id | source | date | claim | stated basis | cite |
|---|---|---|---|---|---|
| P-001 | `2026-09-01-project-breaking-points.md`, ranked #1 | 2026-09-01 | "The tests that can see a write/session failure are not in the hook — shared. CI and pre-commit are lint/typecheck/vitest (+build in CI). `packages/data` mocks `.rpc()`. `test:denial` and `test:e2e` are conventions; unset e2e creds skip exit 0. Hole already shipped #78, 465dcf4, 2026-08-27 (~9h15m sales outage, 7× PGRST203)." | measured — `.github/workflows/ci.yml`, `.husky/pre-commit` | ranked #1 |
| P-002 | same, ranked #4 | 2026-09-01 | "Refresh consume-not-deliver signs the device out; admin fail-hard — shared+client. `POST /auth/v1/token` unbounded on purpose (`fetch-shield.ts:30-36`). If GoTrue consumes the refresh token and Set-Cookie never lands, next load past `refresh_token_reuse_interval=10s` hits `refresh_token_already_used`. Client proxy rides teardown for that code; admin proxy has no park." | "high on code; 465dcf4 is a different (surface) incident" | ranked #4 |
| P-003 | same, ranked #5 | 2026-09-01 | "`/reservar` puts a removed member back; any-gym membership paints the wrong week — client. `reclamar_o_crear_cliente` upserts `gym_membership(member)`. `/reservar` re-runs `intentarReclamoPorEmail` when `getEsMiembro` is false. `getEsMiembro` is `gym_membership.limit(1)` — no gym filter. Member of A on B's host skips claim; `resolverMiembroGym` falls back to oldest membership." | measured — `agenda-miembro.ts:139-142`, `reservar/page.tsx` | ranked #5 |
| P-004 | same, ranked #8 | 2026-09-01 | "Activation/mail is a second identity factory; console state is invisible to every guard — shared+client+admin. Lived: Sarahí 34h wedge; desk sale minted a parallel `clientes` row (`auth_user_id NULL`). `registrar_venta` create does not consult `auth.users`. No unique `(gym_id, tel)`. `send-email` not deployed by `git push`. Auth mail 50/h project-wide. Resend Free 100/day. `/auth/confirm` consumes `token_hash` on GET." | asserted from 2026-08-30 Sarahí / 08-21 Camila catalogs; code paths measured where named | ranked #8 |

### 1b. Catalog `F-NN` rows (filtered; `status: unverified` in source as of this gather)

| id | source | date | claim | stated basis | cite |
|---|---|---|---|---|---|
| P-005 | `2026-09-01-breaking-points-catalog.md`, F-01 | 2026-09-01 | owner: shared. "pre-commit + CI are lint/typecheck/vitest (+build in CI). RPC mocked. `test:denial` and `test:e2e` are conventions; unset e2e creds skip exit 0. Hole already shipped #78, 465dcf4, 2026-08-27." | confidence: measured | F-01; validate → `.github/workflows/ci.yml`, `.husky/pre-commit`, `vitest.config.ts`, `AGENTS.md`, `apps/client/e2e/session.spec.ts` |
| P-006 | catalog F-04 | 2026-09-01 | owner: shared+client. "unbounded `POST /auth/v1/token`. If GoTrue consumes RT and cookies never land, next load after 10s is `refresh_token_already_used`. Client rides teardown for that code. Admin proxy has no park." | confidence: high on code; 465dcf4 is a different (surface) incident | F-04; validate → `fetch-shield.ts`, both `proxy.ts`, `config.toml` |
| P-007 | catalog F-05 | 2026-09-01 | owner: client. "`reclamar_o_crear_cliente` upserts `gym_membership(member)`. `/reservar` re-claims when `getEsMiembro` is false. `getEsMiembro` is `limit(1)` with no gym filter. Host miss → oldest membership's week under this host's chrome." | confidence: measured | F-05; validate → `agenda-miembro.ts`, `reservar/page.tsx`, canonical `reclamar_o_crear_cliente.sql`, `inquilino.ts` |
| P-008 | catalog F-08 | 2026-09-01 | owner: shared+client+admin. "desk sale does not consult `auth.users`. No unique `(gym_id, tel)`. Merge runbook delete-before-repoint cascades. `send-email` not deployed by `git push`. Auth 50/h, Resend 100/day. `/auth/confirm` consumes token on GET. Site URL / Turnstile / JWKS / PITR are dashboard-only." | asserted from 2026-08-30 Sarahí / 08-21 Camila catalogs; measured where named | F-08 |
| P-009 | catalog F-11 | 2026-09-01 | owner: admin+client. "`reclamar_o_crear_cliente` matches verified email; phone never claims. `registrar_venta` NEW dups tel OR email unless `p_forzar_nuevo`. No unique tel. Wedged signup + walk-in sale = two rows." | ev: write-path, incidents | F-11 |
| P-010 | catalog F-12 | 2026-09-01 | owner: client+admin. "default `signOut()` is global. One 'cerrar sesión' deleted every device. Four call sites now pass `scope:'local'`. No unit test." | confidence: high (ADR-0016 amendment; critic: under-ranked) | F-12 |
| P-011 | catalog F-13 | 2026-09-01 | owner: shared. "no exhaustiveness guard (unlike fetch-shield). Mismatch [of cookie options at one of four `@supabase/ssr` sites] = silent empty session." | ev: one-liners | F-13 |
| P-012 | catalog F-14 | 2026-09-01 | owner: shared+client. "fetch-shield tests only that an RPC POST is unbounded. Token URL is untested. Adding a timeout would mass-sign-out." | ev: one-liners | F-14 |
| P-013 | catalog F-15 | 2026-09-01 | owner: client. "ADR-0008 hinge. A Server Action that `resolveTenant(null, h.get('x-gym'))` mints membership. Critic: unknown if already live on a path." | ev: one-liners | F-15 |
| P-014 | catalog F-17 | 2026-09-01 | owner: admin/client. "`getSession()` is cookie, not verified JWT. App/proxy pages untested. DAL fakes implement `getClaims` only." | ev: one-liners | F-17 |
| P-015 | catalog F-18 | 2026-09-01 | owner: shared. "test only `importKeys` the JWK. Wrong pin during jwks outage = mass bounce." | ev: one-liners, scale | F-18 |
| P-016 | catalog F-19 | 2026-09-01 | owner: shared+client. "no test of the header name [`host` → `x-forwarded-host`]. ADR-0012." | ev: one-liners | F-19 |
| P-017 | catalog F-20 | 2026-09-01 | owner: shared. "git push deploys Vercel, never edge functions. Lived: stale `send-email` v6." | ev: one-liners | F-20 |
| P-018 | catalog F-28 | 2026-09-01 | owner: shared. "`gym` cookie has no maxAge. session cookie vs auth 400d. Host-wins on mapped hosts so impact is preview/`?gym=`." | ev: session | F-28 |
| P-019 | catalog F-29 | 2026-09-01 | owner: admin+shared. "Admin proxy fail-hard; no admin e2e. no park, no try/catch, no `session.spec` twin." | ev: session | F-29 |
| P-020 | catalog F-30 | 2026-09-01 | owner: client. "`/auth/confirm` consumes `token_hash` on GET. prefetch/scanner burns the only link (Sarahí FC-22)." | ev: incidents, half-fail | F-30 |
| P-021 | catalog F-31 | 2026-09-01 | owner: client. "Signup re-POST destroys the previous confirmation link. `UNIQUE(user_id, token_type)` — four mails, four dead `flow_state` (Sarahí)." | ev: half-fail, incidents | F-31 |
| P-022 | catalog F-32 | 2026-09-01 | owner: client. "`activar-cuenta` `createUser` then link. Cut after `createUser` → orphan confirmed user; retry `cuenta_existente`. Cut after `generateLink` → burned recovery token." | ev: half-fail | F-32 |
| P-023 | catalog F-33 | 2026-09-01 | owner: shared. "`send-email` hook: Resend 4xx mapped to HTTP 200. GoTrue will not retry. Burned OTP, no inbox. 5xx after Resend 200 = duplicate mail." | ev: half-fail | F-33 |
| P-024 | catalog F-39 | 2026-09-01 | owner: shared. "`resolveTenant` 500 FIFO / isolate, 60s TTL; admin+client keys double the slots. Unbounded POST `gym_id_por_host` after eviction." | map: scale table | F-39 |
| P-025 | catalog F-40 | 2026-09-01 | owner: client. "no `ventas(cliente_id)` index in migrations [for `mi_membresia` seq-scan]." | ev: scale | F-40 |
| P-026 | catalog F-42 | 2026-09-01 | owner: shared. "`vercel.json` pins functions `pdx1`. `proxy.ts` cannot be pinned. Unbounded POSTs from IAD." (~7% of middleware entered IAD post-pin) | ev: scale, incidents | F-42 |
| P-027 | catalog F-43 | 2026-09-01 | owner: shared. "266s stall on the retry still hangs the render. Comment admits it [fetch-shield GET/HEAD 8s + untimed retry]." | ev: half-fail | F-43 |
| P-028 | catalog F-44 | 2026-09-01 | owner: client. "Invite codes never expire. ADR-0015 rejected TTL. HMAC has no `exp`. OTP is 3600s." | map: 90-day table | F-44 |
| P-029 | catalog F-49 | 2026-09-01 | owner: shared+client. "Console-only: Site URL, allow-list, Turnstile hosts. Site URL was a retired host (`https://red.ibookit.lat`). Guards cannot see dashboard." | ev: incidents | F-49 |
| P-030 | catalog F-50 | 2026-09-01 | owner: client. "Dual member hosts, disjoint `__Host-` jars, no canonical redirect. `red.ibookit.lat` vs `www.redfunctionaltraining.com`. Sign in on one, signed out on the other." | ev: session, incidents | F-50 |
| P-031 | catalog F-53 | 2026-09-01 | owner: shared. "`token_refresh` 150 / 5 min / IP. gym Wi-Fi NAT can look like one IP." | ev: session | F-53 |
| P-032 | catalog F-54 | 2026-09-01 | owner: shared+admin. "Revocation lag ≤3600s (ES256 local verify). ADR-0016 trap sprung. Fired operator can look authed for up to 1h." | map: numbers table | F-54 |
| P-033 | catalog F-55 | 2026-09-01 | owner: client. "Staff identity on the client app → hollow week. staff membership makes `getEsMiembro` true, skip self-heal, `resolverMiembroGym` may show a hollow member week." | ev: session | F-55 |
| P-034 | catalog F-56 | 2026-09-01 | owner: admin. "Duplicate merge delete-before-repoint cascades. FKs `ON DELETE CASCADE` from `clientes`; runbook warning still present." | ev: write-path | F-56 |
| P-035 | catalog F-57 | 2026-09-01 | owner: client. "Magic-link `token_hash` single-use / double-click. magic-link send + confirm consume. Prefetch of confirm URL." | ev: half-fail | F-57 |
| P-036 | catalog F-58 | 2026-09-01 | owner: shared. "One Resend key = SMTP password; shared bounce budget. 07-22: 28 invites, 1 hard bounce in 18 = 5.6% vs <4% shutdown." | ev: incidents | F-58 |
| P-037 | catalog F-59 | 2026-09-01 | owner: shared. "Wedge alerts drop rows older than 30 days. `registros_atorados` window in `alertas/`." | map: 90-day table | F-59 |

### 1c. One-liners (green-and-fatal single edits; filtered — full list is 1–15 in the evidence doc)

| id | source | date | claim | stated basis | cite |
|---|---|---|---|---|---|
| P-038 | `2026-09-01-breaking-points-evidence.md`, one-liners #1 | 2026-09-01 | "Drop `cookieOptions: SUPABASE_COOKIE_OPTIONS` at one of: admin `proxy.ts`, client `proxy.ts`, `packages/data/src/server/supabase.ts`, `packages/data/src/client.ts`. Or add a fifth `createBrowserClient` without it." | reasoning — exact plausible edit; `pnpm test`/pre-commit stay green | one-liners #1 |
| P-039 | same, #2 | 2026-09-01 | "In `fetch-shield.ts`, before the GET/HEAD branch, timeout `/auth/v1/token`. Test only asserts `/rpc/registrar_venta` unbounded (`fetch-shield.test.ts` ~L79-92)." | reasoning | one-liners #2 |
| P-040 | same, #3 | 2026-09-01 | "`signOut()` without `{ scope: 'local' }` — e.g. `apps/client/.../cerrar-sesion-link.tsx`. Default global." | reasoning; lived incident cited elsewhere (2026-08-24) | one-liners #3 |
| P-041 | same, #4 | 2026-09-01 | "`apps/client/src/app/registro/actions.ts` `resolveTenant(null, h.get('x-gym'))` instead of `host`." | reasoning | one-liners #4 |
| P-042 | same, #7 | 2026-09-01 | "`getClaims()` → `getSession()` on admin `proxy.ts` or `reservar/page.tsx`. `getSession(` in apps is comments only. DAL fakes implement `getClaims`." | reasoning | one-liners #7 |
| P-043 | same, #8 | 2026-09-01 | "Replace `JWKS_FALLBACK` kid/x/y with another well-formed P-256 JWK. Test only `importKey`s." | reasoning | one-liners #8 |
| P-044 | same, #9 | 2026-09-01 | "`headers.get('host')` → `get('x-forwarded-host')` at proxy, `auth/confirm/route.ts`, `registro/actions.ts`. No test of the header name." | reasoning | one-liners #9 |
| P-045 | same, #10 | 2026-09-01 | "`.husky/pre-push` last line unconditional `exit 0`." | reasoning | one-liners #10 |

### 1d. Evidence-doc cites (filtered from write-path/claim, session, half-fail, scale, incidents)

| id | source | date | claim | stated basis | cite |
|---|---|---|---|---|---|
| P-046 | evidence.md, write-path § Claim | 2026-09-01 | "`reclamar_o_crear_cliente`: match verified email; `v_n != 1` → INSERT. Already claimed: membership `ON CONFLICT DO NOTHING`, `reclamado:false` — idempotent. `reclamar_por_codigo`: `FOR UPDATE` on code; spent code errors. Doors swallow via `intentarReclamo*`. `registrar_venta` NEW: dup check tel OR email unless `p_forzar_nuevo`." | measured — canonical SQL line refs given | write-path § Claim |
| P-047 | evidence.md, write-path § Claim | 2026-09-01 | "Children of `clientes` `ON DELETE CASCADE` — merge runbook `docs/runbooks/duplicate-member-merge.md`: delete-before-repoint wipes ventas/asistencias/reservas." | measured | write-path § Claim |
| P-048 | evidence.md, session § Two proxies | 2026-09-01 | Table comparing admin vs client `proxy.ts`: admin has no `try/catch` on `getClaims` (throws 500s) and no fail-soft on GoTrue 5xx; client has both. Admin has `x-ruta`; client does not. Admin cookie teardown "ride everything"; client "park deletions-only unless dead-session codes." | measured — line/byte counts given for both files (2026-09-01) | session § Two proxies |
| P-049 | evidence.md, session § Two proxies | 2026-09-01 | "Dead codes (client): `refresh_token_not_found`, `refresh_token_already_used`, `session_not_found`, `session_expired` (`CODIGOS_SESION_MUERTA`). `esBorradoTotal`: every cookie in the batch is empty — not `any`." | measured — `proxy.ts` + `proxy.test.ts` | session § Two proxies |
| P-050 | evidence.md, session § Two proxies | 2026-09-01 | "Prefetch exclusion was investigated and rejected (client proxy comments ~90-97): skipping rotation in the proxy relocates it into RSC `setAll` (no-op) and manufactures consume-not-deliver." | measured — code comment | session § Two proxies |
| P-051 | evidence.md, session § Cookies | 2026-09-01 | "prod `{ name: '__Host-sb-auth-token', secure: true }`. Four sites must match (#209). No exhaustiveness guard. `__Host-` forbids Domain — `red.ibookit.lat` and `www.redfunctionaltraining.com` are disjoint jars. `gym` cookie: no maxAge → browser-session cookie; auth cookie SDK maxAge 400 days. `httpOnly:false` → ITP ~7-day cap. `SECURE_COOKIES = isProd` is a boolean, intentional." | measured — `cookie-options.ts` | session § Cookies |
| P-052 | evidence.md, session § Numbers | 2026-09-01 | `jwt_expiry` 3600; `refresh_token_reuse_interval` 10; `otp_expiry` 3600; tenant cache 60_000ms/500 FIFO; lived session 21 days, `not_after=null` (2026-08-02); 465dcf4 = 8 days healthy rotation, password form 37s after a 200 refresh; `token_refresh` 150/5min/IP; `EXPIRY_MARGIN` ~90s (auth-js). | measured/asserted (dated probes named) | session § Numbers |
| P-053 | evidence.md, session § Identity | 2026-09-01 | "`getEsMiembro`: `.select('gym_id').limit(1)` — any gym. `resolverMiembroGym`: host match else oldest membership. `/reservar` calls `intentarReclamoPorEmail` when not a member. `signOut({scope:'local'})` at 4 sites (ADR-0016 amendment 2026-08-24). Default without scope is global. No test. `staff_gym()` is `order by gym_id limit 1`. Staff on client: `getEsMiembro` true → skip self-heal → hollow week. `/entrar` and `/registro` redirect a live session to `/reservar`. No account switcher. Admin has no `e2e/session.spec.ts` twin." | measured — file/line refs given | session § Identity |
| P-054 | evidence.md, half-fail table | 2026-09-01 | "`activar-cuenta` `createUser` then `generateLink` then `verifyOtp` — cut after `createUser` = orphan user; cut after `generateLink` = burned recovery token / `cuenta_existente` on retry." | modelled from RPC/edge-fn order | half-fail table |
| P-055 | evidence.md, half-fail table | 2026-09-01 | "`send-email` hook: OTP already minted before Resend call. `respuestaEnvio`: null/429/≥500 → HTTP 503 (retry); other incl. 4xx → HTTP 200. 4xx swallowed = burned OTP; 503 after 200 = duplicate mail." | measured — hook logic described | half-fail table |
| P-056 | evidence.md, half-fail table | 2026-09-01 | "signup `registrarSocio`: re-POST rotates previous confirmation token (`UNIQUE(user_id, token_type)`)." | measured/modelled | half-fail table |
| P-057 | evidence.md, half-fail table | 2026-09-01 | "magic link: send then confirm GET consumes `token_hash`; prefetch/double-click burns it." | modelled | half-fail table |
| P-058 | evidence.md, half-fail table | 2026-09-01 | "proxy refresh: GoTrue rotation commits; Set-Cookie may not land → `already_used`." | modelled | half-fail table |
| P-059 | evidence.md, half-fail § note | 2026-09-01 | "`completarActivacion`: password first, then best-effort claim. Abandoned after password, before claim: logged in, code may still be live." | measured/modelled | half-fail § note |
| P-060 | evidence.md, scale § | 2026-09-01 | "`resolveTenant`: 500×60s per isolate; keys `${app}|${host}` double the slots. Two apps × many isolates." | measured — constants named | scale § |
| P-061 | evidence.md, scale § | 2026-09-01 | "`mi_membresia`: `from ventas where cliente_id = v_cli order by created_at desc`. Migrations index `ventas` on `gym_id` and `(gym_id, fecha)` only — no `ventas(cliente_id, …)`." | measured | scale § |
| P-062 | evidence.md, incidents § 465dcf4 | 2026-08-21 | "auto-refresh 8 days; 37s after a 200 rotation, password form. Cause: `/entrar` + CTAs, not token race. Shield: `apps/client/e2e/session.spec.ts` — not in CI; unset creds skip exit 0. One-line fix: delete `if (data?.claims?.sub) redirect('/reservar')` in `entrar/page.tsx`." | asserted from dated audit (`2026-08-21-login-session-persistence-analysis.md`) | incidents § 465dcf4 |
| P-063 | evidence.md, incidents § 2026-08-29 | 2026-08-29 | "IAD jwks 266s: pin `pdx1` + fetch-shield. Residuals: POST unbounded, GET retry untimed, middleware unpinnable, JWKS pin must rotate with keys." | asserted from dated audit | incidents § 2026-08-29 |
| P-064 | evidence.md, incidents § 2026-08-30 | 2026-08-30 | "Sarahí 34h activation wedge. First click: zero `/auth/v1/verify`. Catch-all told her to use a password that cannot work. Re-POST `/registro` destroyed the previous link. Four mails, four `flow_state` with `auth_code_issued_at NULL`. Desk sale 22:22 minted a second identity. Repair = raw SQL. 4 of 53 auth accounts wedged; one 17 days. Alert cron watched `invalid_grant` + send-email non-2xx — both 0." | asserted from dated audit (`docs/superpowers/plans/2026-08-30-auth-door-shield-plan.md`) | incidents § 2026-08-30 |
| P-065 | evidence.md, incidents § Camila | 2026-08-21 | "invited 08-14, 'wrong password' against a nonexistent account." | asserted | incidents § Camila |
| P-066 | evidence.md, incidents § 2026-08-24 | 2026-08-24 | "Owner asked for password after a week away. Live `auth.sessions` deleted, not expired. `signOut` default `scope:'global'`. Amendment: all 4 sites `scope:'local'`." | asserted | incidents § 2026-08-24 |
| P-067 | evidence.md, incidents § 2026-08-02 | 2026-08-02 | "All six `auth.sessions` `not_after=null`. Session from 2026-07-11 still refreshing 2026-08-01 (21 days). Free tier cannot time-box. Pro 30d/14d rescinded for members (same Auth project)." | asserted — probe date given | incidents § 2026-08-02 |
| P-068 | evidence.md, incidents § 2026-08-28 | 2026-08-28 | "`atp404951` unclaim: nulling `auth_user_id` does not log anyone out. Live session still short-circuited to VINCULAR." | asserted | incidents § 2026-08-28 |
| P-069 | evidence.md, incidents § Mail bucket | 2026-09-01 | "Auth 50/h project-wide. GoTrue floor 60s/address = 60/h, above the platform budget. Resend Free 100/day, 3,000/month, bounce <4%. 07-22 audit: 28 invites ever; 1 hard bounce in 18 = 5.6%. One Resend key on invite + receipt + auth hook + alert cron, also the SMTP password. No outbox, no retry." | asserted from 07-22 audit | incidents § Mail bucket |
| P-070 | evidence.md, incidents § Edge deploy | 2026-08-04 | "`.husky/pre-push` blocks a push touching `supabase/functions/**` unless `EDGE_DEPLOY_OK=1`. Lived: stale `send-email` v6." | asserted — AGENTS.md, 2026-08-04 | incidents § Edge deploy |

---

## 2. 2026-09-02 Marce triage (`docs/FIndings/2026-09-02-marce-triage.md`)

| id | source | date | claim | stated basis | cite |
|---|---|---|---|---|---|
| P-071 | marce-triage §7, H1 | 2026-09-02 | "Invite link bound to the auth user deleted on 09-01 — REFUTED." | live SQL — `codigo`/`firma` byte-identical in mails #10/#11/#13 across the deletion | §7 H1 |
| P-072 | same, H2 | 2026-09-02 | "Her rail hit the `send-email` hook 500 — REFUTED." | live logs — all four `run_hook` lines log `error:null`; the 500 belongs only to `delivered@resend.dev` | §7 H2 |
| P-073 | same, H3 | 2026-09-02 | "`clientes.auth_user_id` orphaned at a deleted user — REFUTED." | live SQL join — `auth_user_exists:true`, single cliente row | §7 H3 |
| P-074 | same, H4 | 2026-09-02 | "token / firma TTL exceeded — REFUTED." | live data — firma never changes; failure mode is rotation, not expiry | §7 H4 |
| P-075 | same, H5 | 2026-09-02 | "Tenant/host mismatch after the 08-28 cutover — REFUTED." | live data — every mailed link is on `www.redfunctionaltraining.com` (`es_principal`) | §7 H5 |
| P-076 | same, H6 | 2026-09-02 | "`/registro` → `/activar` inside 60s → `429 over_email_send_rate_limit` → `cuentaExistenteFallo` error screen — CONFIRMED." | live logs — two 429 lines at 19:25:17/19:25:27, sole occurrences in the 24h window | §7 H6 |
| P-077 | same, H7 | 2026-09-02 | "Six identical-subject mails, only the newest live; opening a stale one → `otp_expired` → `/entrar?error=token-rechazado` — PLAUSIBLE, mechanism proven, her click not." | `auth.one_time_tokens` empty; 6 delivered mails with 3 distinct `token_hash`; Resend has no click tracking so her specific click is unrecoverable | §7 H7 |
| P-078 | marce-triage §9, code-fix #1 | 2026-09-02 | "`cuentaExistenteFallo` swallows a 429 and tells the member the wrong thing. `activar/actions.ts:94` maps every `enviarMagicLink` failure to one screen; a throttle is a wait-30s, not a no-salió-el-correo. Surface `over_email_send_rate_limit` distinctly with a countdown." | reasoning from evidenced defect | §9 #1 |
| P-079 | same, code-fix #2 | 2026-09-02 | "`/activar` does not detect that the same person just signed up at `/registro` seconds earlier. The `cuenta_existente` branch should recognise an unconfirmed account for the same address and say 'ya te mandamos un correo hace un momento' instead of spending another `/otp`." | reasoning | §9 #2 |
| P-080 | same, code-fix #3 | 2026-09-02 | "Six mails with the identical subject 'Confirma tu cuenta' are indistinguishable in an inbox, and only the newest works. Number them, timestamp the subject, or state the send time in the body." | reasoning | §9 #3 |
| P-081 | same, code-fix #4 | 2026-09-02 | "`/registro` and `/activar` are two doors to one outcome with no cross-links. A member holding an invite has no reason to be at `/registro`; a member at `/registro` whose email matches an unclaimed coded cliente should be routed to the invite rail." | reasoning | §9 #4 |
| P-082 | same, code-fix #5 | 2026-09-02 | "The 6-digit OTP fallback (`bloqueCodigo`, `correo.ts:90-96`) renders only for `emailActionType==='signup'`, and the `/codigo` landing exists — her magic-link mails never carried a usable fallback. Re-evaluate now that `/codigo` is live." | reasoning | §9 #5 |
| P-083 | same, code-fix #6 | 2026-09-02 | "`e2e/signup.spec.ts` runs against a host GoTrue clamps, so it will fail forever on `500: Invalid payload sent to hook`. Either add the e2e host to the Supabase redirect allow-list or pass an allowed `emailRedirectTo`." | reasoning from evidenced test-harness gap | §9 #6 |
| P-084 | same, code-fix #7 | 2026-09-02 | "`auth.audit_log_entries` is empty and the log stream holds 24h. There is no durable auth forensics trail; the Resend ledger was the only record of 09-01 02:50 and 14:12. Ship a log drain or an application-side auth event table." | live SQL — `select count(*) from auth.audit_log_entries` = 0 | §9 #7; §1 |
| P-085 | same, code-fix #8 | 2026-09-02 | "Resend open/click tracking is off, so 'did the member open the link' is unanswerable during an incident. Turn it on — this is the single experiment that would have settled H7." | reasoning from evidenced gap | §9 #8 |
| P-086 | marce-triage §3 | 2026-09-02 | "Every mail to her is `last_event:'delivered'`. Not one bounce, not one deferral." (13 mails listed, incl. 3 pre-24h-log-window Resend-only records) | live Resend ledger — `GET /emails?limit=100` | §3 |
| P-087 | same §3 | 2026-09-02 | "That is six 'Confirma tu cuenta' mails with identical subject and identical sender in one inbox. Only #13 was live." | live Resend ledger | §3 |
| P-088 | marce-triage §2 | 2026-09-02 | "Cross-check — she is the ONLY account that hit the `over_email_send_rate_limit` 429 in the entire 24h window (2 rows, both hers, 19:25:17 and 19:25:27)." | live log query | §2 |
| P-089 | marce-triage §1 | 2026-09-02 | "`auth.audit_log_entries` — EMPTY (0 rows, table-wide). The durable audit table this project version writes nothing to. The audit ledger lives only in the log stream, capped at a 24h window. Everything before 2026-09-01 ~17:00Z is unrecoverable from logs — the Resend ledger is the only surviving record of that period." | live SQL | §1 |

---

## 3. 2026-07-13 respaldo-mensual-base-defects (on-surface rows only)

| id | source | date | claim | stated basis | cite |
|---|---|---|---|---|---|
| P-090 | `2026-07-13-respaldo-mensual-base-defects.md` §5 | 2026-07-13 | "`reclamar_o_crear_cliente(p_gym_id uuid)` mints a membership in ANY gym you name. `SECURITY DEFINER`, `EXECUTE` granted to `authenticated`, gym id is a caller-supplied parameter, nothing binds `p_gym_id` to the caller's host or existing gym. Chain: read `gym` (world-readable) → pick a competitor's `gym_id` → call the RPC → hold a `member` row there → `is_member_of` goes true." | "verified live by me, not just by the agent" | §5, points 1–3 |
| P-091 | same, §11 D2 | 2026-07-13 | "D2 — `reclamar_o_crear_cliente(p_gym_id)` lets any authed user mint a membership in any gym. RULING (owner, 2026-07-13): FIX NOW, this release. Bind `p_gym_id` to the resolved tenant. Migration → takes the `test:denial` gate." | owner ruling, dated | §11 D2 |

---

## 4. Memory notes (`.claude/projects/…/memory/`, 18 files scoped)

| id | source | date | claim | stated basis | cite |
|---|---|---|---|---|---|
| P-092 | `auth-door-incident-shield-plan.md` | 2026-08-30 | Sarahí self-signup wedge, root-cause chain: unlogged first-click failure → `/auth/confirm` catch-all unlogged → no resend door existed → `/registro` resubmit silently rotates the one token (retry IS the damage). H-A (hook wrong host) REFUTED. Desk sale minted parallel identity. | memory (session-derived); full plan cross-referenced | `auth-door-incident-shield-plan.md`; full: `docs/superpowers/plans/2026-08-30-auth-door-shield-plan.md` (24 FC points) |
| P-093 | same | 2026-08-30 | Wave 1 SHIPPED live ~20:15 local: `send-email` v8 deployed, pushed `bed1f7d..1756675`, e2e 6/6 (3 session + 3 signup-rail), denial 54/54 local docker, Sarahí verified logged-in via Playwright on live, claim self-healed. Still open: `/registro` enumeration tradeoff, `&correo=` pre-fill loss, persisted alert counter, Vercel Pro cron confirm. | memory (session-derived, dated) | `auth-door-incident-shield-plan.md` |
| P-094 | `login-session-persistence-shipped.md` | 2026-08-21 | "Root cause (evidence-corrected): the auth surface was session-blind — `/entrar` and `/registro` never checked claims and drawer CTAs sent signed-in members to `/registro`. Katya: 8 days of successful rotations, then a password login 37s after a 200 refresh. Live logs: ZERO `invalid_grant` in 24h; asymmetric JWT keys are LIVE." Shipped 465dcf4: redirect live sessions to `/reservar`, proxy fail-soft (`esBorradoTotal`), rate-limit copy, password trim parity, `SinMembresia` sign-out escape hatch. C1 prefetch exclusion REJECTED. | memory (session-derived, dated) | `login-session-persistence-shipped.md`; full: `docs/Context/2026-08-21-login-session-persistence-analysis.md` |
| P-095 | same | 2026-08-25 | "Camila Rodríguez RESOLVED 2026-08-25: self-registered via `/registro` (08-24) with password but never confirmed → `email_not_confirmed` wall; fixed by manual SQL provision on live. STILL OPEN: Camila Reyes same accountless state. Flow gap: unconfirmed-signup dead end — no resend-confirmation CTA; expired confirm link (>24h) strands the member." Same-day shields @97fbbe5: first e2e infra (`pnpm test:e2e`, 3 tests), daily alert cron (unarmed until owner sets Vercel env vars). | memory (session-derived, dated) | `login-session-persistence-shipped.md` |
| P-096 | `activation-security-audit.md` | 2026-07-22 | "3 HIGH (all 'the code/link is the only credential'): H1 `reclamar_por_codigo` binds ANY verified session to a coded row with no caller-email check, directly PostgREST-callable. H2 `/auth/confirm` runs `reclamarPorCodigo` on any URL `codigo`, even on a forgot-password session. H3 new-account path `createUser({email_confirm:true})` = no inbox proof; invite link carries code + `&correo=` — a forwarded link is a full activation credential (deliberate #126)." Launch gate: verify Turnstile keys are non-default. | memory (audit-derived, 26 agents, 4 lenses + skeptic verify); full report `docs/Context/2026-07-22-activation-security-audit.md` | `activation-security-audit.md` |
| P-097 | same | 2026-07-24 | "Status (updated): remediation SHIPPED + LIVE. H1/H2v1 closed by the firma migration (`reclamar_por_codigo(p_codigo, p_firma)`); magic-link UX + `vincular` short-circuit deployed; H2v2 ruled option (b) — `/registro` claim arm deleted, `/activar` sole invite door; H3 remains the accepted #126 tradeoff." | memory, dated | `activation-security-audit.md` |
| P-098 | `activation-rails-two-paths.md` | 2026-07-27 | "`auth.users` is global to the platform, not per gym. `/activar` splits into two production rails: fresh-provision (no auth account → `admin.createUser` → password → claim, zero extra mail) vs `cuenta_existente` (account exists anywhere → `createUser` fails `email_exists` → passwordless sign-in link, never a reset, binds this gym's membership). Verified 2026-07-27: all 18 real RED members have zero auth accounts → every one walks fresh-provision." | memory (verified live, dated) | `activation-rails-two-paths.md` |
| P-099 | `activation-cutover-shipped.md` | 2026-07-24 | "Cutover LIVE 2026-07-24: `reclamar_por_codigo(p_codigo,p_firma)` live (one-arg overload dropped), `test:denial` 37/37 on scratch. H2v2 RULED option (b), shipped same cutover: `/registro` codigo-claim arm deleted (+22/−171); `/activar` sole invite door. Live-verified magic-link rail walk end to end. Password model: new accounts set password at `/activar/contrasena` BEFORE claim; existing accounts keep theirs." | memory (session-derived, dated) | `activation-cutover-shipped.md` |
| P-100 | `single-email-activation-shipped.md` | 2026-07-15 | "Single-email member activation COMPLETE — owner live walk PASSED 2026-07-15. `/activar?codigo=` (typed email + Turnstile) → edge fn `activar-cuenta` (HMAC firma over `CODIGO:email`, `verify_jwt:false`, `createUser({email_confirm:true})`, mints `generateLink({type:'recovery'})`, token consumed server-side same request — no expiry cliff, scanner-proof) → `/activar/contrasena` → `reclamar_por_codigo` → `/reservar`." Adversarial review fixes: pre-existing accounts get `cuenta_existente` → inbox-proof reset, never a server-consumed token. | memory (session-derived, dated) | `single-email-activation-shipped.md` |
| P-101 | `unclaim-cliente-recipe.md` | 2026-08-28 | "Clearing `clientes.email` alone is impossible AND insufficient — four layers block it. `actualizar_cliente` hard-raises on an email edit to a claimed row. Never `set local role authenticated` (re-arms RLS, silently no-ops). Never delete `gym_membership` (its trigger nukes sessions globally — leave it, `reclamar_por_codigo` upserts `on conflict do nothing`). Never delete `auth.users` unless the address's account owns nothing — it is unnecessary and irreversible." Three traps: (1) the `vincular` short-circuit signs nobody out; (2) invite mail holds the OLD host by design (`created_at asc` ordering); (3) which rail fires is decided by `auth.users`, not the cliente row — a password screen appearing on the `cuenta_existente` rail is structurally impossible and is an alarm if seen. | memory (recipe, dated 2026-08-28 application) | `unclaim-cliente-recipe.md` |
| P-102 | `cross-tenant-login-verdict.md` | 2026-08-02 | "Signing into the RED admin host with a Forge admin account works because nothing compares the host's gym against the session's memberships — `apps/admin` reads `x-gym` zero times and the auth gate at `proxy.ts:71` is a boolean. No data crosses (8 red-team chains died at the read boundary; RLS correct for the single-gym case). Three things found that are NOT the login defect and are live: `gym.legal_name`/`owner_user_id` readable by any authenticated identity; 15 catalog tables `USING(true)` to anon; `auth.audit_log_entries` empty." Epic #203 opened (#204–#219). | memory (7-agent cross-examine, then code-reviewed, committed `ce10398`) | `cross-tenant-login-verdict.md`; full: `docs/Context/2026-08-02-cross-tenant-login-cross-examination.md` |
| P-103 | `multigym-rpc-roulette.md` | 2026-07-27 | "CONFIRMED live bug: `mi_membresia()` and `toggle_favorito_tipo()` resolve 'the caller's cliente row' as `select … from clientes where auth_user_id=v_uid limit 1` — no gym filter, no ORDER BY, non-deterministic. A member in two gyms can see/write the other gym's data. Latent as of 2026-07-27: zero multi-gym members exist in prod." | memory (found live, design-audit-derived) | `multigym-rpc-roulette.md` |
| P-104 | `auth-structure-scale-audit.md` | 2026-07-27 | "One global `auth.users` row per email with per-gym `clientes` joined through `gym_membership` is CORRECT (5/5 unanimous). Supabase = fit-with-mitigations (5/5), $0.53–$1.04/gym/month at 3,000 gyms. `ventas` has NO `cliente_id` index and `mi_membresia()` scans it on every plan-card render (only 1/5 auditors caught it). `gym_membership` OR'd permissive policies are structurally unindexable (275,638 seq scans vs 867 idx on a 9-row table). 4 emails already hold `clientes` rows in 2 gyms; 0 claimed in both — wrong-gym reads latent, not live." | memory (5-agent replication panel + orchestrator referee against live prod) | `auth-structure-scale-audit.md`; full: `docs/Context/2026-07-27-auth-structure-scale-audit.md` |
| P-105 | `email-capacity-ceilings.md` | 2026-07-22 | "No queue, no outbox, no retry, no bulk path anywhere. Peak concurrency is 2. Bounce budget binds before throughput: one Resend account/domain for all tenants, bounce<4%/complaint<0.08% are account-level, penalty is suspension without warning (stops invites+receipts+auth mail at once, same key is SMTP password). Supabase auth email limit is 50/hr project-wide by deliberate design (so a burst can't outrun Resend's 100/day). A 429 is indistinguishable from a 500 in `resendTransport` — dropped, no retry. A sixth auto-send rail: editing a ficha to set an email on an unclaimed row fires an invite with no confirmation." | memory (3 recon + capacity model + 3 adversarial verifiers, all returned PARTIALLY_WRONG on first pass — corrected) | `email-capacity-ceilings.md`; full: `docs/Context/2026-07-22-invite-mail-capacity-audit.md` |
| P-106 | `email-deliverability-lane.md` | 2026-09-01 | "20-agent verified research: buy nothing, stay on Resend, build the Svix-verified bounce webhook → `clientes.correo_estado` + desk badge. F4 (DMARC ratchet) and F5 (one-click unsub) are NEVER as scoped. Worst platform risk: one Resend account, AUP account-wide thresholds — one gym's stale-list blast can kill all tenants' mail." Marce ticket noted RESOLVED (this session's own triage superseded that read — see §2 above). | memory (20-agent research, dated) | `email-deliverability-lane.md`; full: `docs/research/2026-09-01-email-deliverability-build-vs-buy.md`, `docs/Context/2026-09-01-email-deliverability-HANDOFF.md` |
| P-107 | `non-ascii-email-invite-bug.md` | 2026-08-29 | "RED owner's 'NO PUDIMOS ENVIAR LA INVITACIÓN' = Resend `422` on a `ñ` email (probed live with the prod key). The desk sale form was the only door that didn't validate email. Fix LOCAL (not pushed as of that note): `isEmailValido` ASCII-only, `crearVentaSchema` refine, `resendTransport` maps 422 to `error:'correo-invalido'`." | memory (live-probed, dated) | `non-ascii-email-invite-bug.md` |
| P-108 | `send-email-hook-shipped.md` | 2026-07-10 | "#75 CLOSED — hook LIVE. Owner walk passed end to end. Edge function v5 (hook 200 responses must be JSON `{}` — GoTrue rejects text/plain). Custom SMTP must STAY ON even though unused for rendering: without it the auth rate limit reverts 50/hr → ~2/hr. Unmapped-host signup correctly refuses." | memory (session-derived, dated) | `send-email-hook-shipped.md` |
| P-109 | `member-reachability-todo.md` | 2026-08-19 | "Silent permanent lockout: mail security scanners (Defender Safe Links, Proofpoint, Mimecast) prefetch emailed URLs. `auth/confirm/route.ts:104-110` consumes the single-use `token_hash` on a bare GET, and `entrar/page.tsx:11` never reads `searchParams` — member sees a blank login form, nothing logged. Fix is Supabase's own recommendation: a POST interstitial." Also: all 9 TLS certs expire 2026-10-07, renewal never run. | memory (17-agent root-cause, dated); full: `docs/Context/2026-08-19-member-reachability-todo.md` | `member-reachability-todo.md` |
| P-110 | `test-member-email-roster.md` | 2026-08-28 (addendum, supersedes 2026-08-01 body) | "`atp404951@gmail.com` is NO LONGER a fresh-provision address as of 2026-08-04 (auth account created that date) — it walks `cuenta_existente`. Un-claim applied 2026-08-28 for the custom-domain invite re-walk; auth account subsequently DELETED (owner-approved) so the password-setting flow could be exercised — self-healing, since completing fresh-provision mints a new `auth.users` row." | memory (live-verified, dated, superseding an earlier snapshot) | `test-member-email-roster.md` |
| P-111 | `epic-203-tenant-in-effect.md` | 2026-08-04 | "#204, #208–#219 CLOSED. #205 repro DONE (Forge staff login on RED demo host renders Forge data + footer, no breach). #206 Auth Redirect-URL allow-list VERIFIED host-scoped, then trimmed to 6 single-host entries. #207 stray Vercel domain removed. Epic stays OPEN only on #220/#221 (member-side gym scoping, membership re-mint)." | memory (session-derived, dated) | `epic-203-tenant-in-effect.md` |
| P-112 | `adr-0013-rls-per-row-claim-is-false.md` | 2026-07-28 (memory itself resolved/superseded) | "The correction to ADR-0013 shipped 2026-07-13 (`20260714080000_rls_uncorrelated_predicates.sql`). Still true and actionable: `(select is_staff_of(gym_id))` is a correlated SubPlan per row, so `.eq('gym_id', gymId)` on staff reads is a real perf requirement. Coverage is only ~33% (40 of ~122 non-test `.from()` call sites; 10–11 DAL modules have zero, incl. `agenda.ts`). Measured 2026-07-28 at 611k rows: `.eq('user_id', uid)` on `gym_membership` reads is worth 667× (1,056ms → 1.58ms)." | memory (measured, dated) | `adr-0013-rls-per-row-claim-is-false.md` |

---

## 5. ADR / handoff claims (`grep -riE 'activar|registro|send-email|proxy\.ts|__Host|refresh' docs/Context docs/adr`)

### ADRs

| id | source | date | claim | stated basis | cite |
|---|---|---|---|---|---|
| P-113 | `docs/adr/0008-…md` §Decision/hinge | 2026-06-29, amended 2026-08-02 | "The `proxy.ts` host resolves brand and UX only. It is NEVER the authorization boundary. Isolation is enforced in Postgres, by RLS policies keyed to `gym_membership` + role." | ADR decision (accepted, amended) | ADR-0008 §"The hinge" |
| P-114 | same, §Amendment 2026-08-02 | 2026-08-02 | "'Presentation-only' also forbids narrowing, which the code already violates for correctness (`x-gym` passed into `/reservar`'s resolver) and outright (`auth/confirm/route.ts` feeds the host-resolved gym into `reclamar_o_crear_cliente`, which inserts a `gym_membership` row — the host mints membership, not merely mispaints one). Amendment: 'the tenant-in-effect may only NARROW to a tenant the caller already holds membership for, never widen; derived from host reconciled against membership; when the host names no membership, resolve explicitly rather than guessing.'" | ADR amendment, cites `docs/Context/2026-08-02-cross-tenant-login-cross-examination.md` + `-route.md`, epic #203 | ADR-0008 §Amendment 2026-08-02 |
| P-115 | `docs/adr/0009-…md` §Decision, §Claim-by-match | 2026-06-29 | "Identity is two-tier: operator and member are distinct roles in distinct profile tables joined by `gym_membership`. `member.auth_user_id` is nullable on purpose and stays nullable permanently. Claim-by-match: verified-email/phone match against pre-existing `member` rows before creating anything — prevents duplicate members and orphaned `paquetes` balances." | ADR decision (accepted) | ADR-0009 §Decision |
| P-116 | same, §Amendment 2026-07-02 | 2026-07-02 | "A claim executes only on a verified-email match; phone is demoted to a reconciliation hint (phone-only claim would let anyone who knows a member's phone claim their balance). Ambiguous match (>1 unclaimed row) → create, never guess. Mechanism: one atomic SECURITY DEFINER RPC, match→set auth_user_id→insert gym_membership in one transaction." | ADR amendment (Phase-3 grill ruling) | ADR-0009 §Amendment 2026-07-02 |
| P-117 | same, §Amendment 2026-07-05 | 2026-07-05 | "Open enrollment is accepted debt (I1): `reclamar_o_crear_cliente` grants EXECUTE to every `authenticated` session and takes `p_gym_id` as a caller argument — any verified-email user can mint a `clientes`+`gym_membership` row into ANY gym, trivially scriptable (`gym` rows are anon-readable). Bounded: no PII exposed, no cross-member reads, no writes to other tenant-table surface. Accepted while Forge is sole live gym; re-open when a second gym goes live OR the first abuse report lands. A naive 'check p_gym_id matches caller' guard is called out as a non-fix." | ADR amendment (owner ruling) | ADR-0009 §Amendment 2026-07-05 |
| P-118 | same, §Amendment 2026-07-15 | 2026-07-15 | "Invited-member activation door: an invited member activates against a pre-confirmed account (`/activar`), not the self-registration form — the invitation mail IS the verification. Server-consumed activation tokens are minted ONLY for accounts the activation itself provisions; a pre-existing account always requires inbox proof (`cuenta_existente` → recovery rail), because minting a token for an account the activation didn't create would hand a live session to that account with no inbox proof — a hostile gym-B operator could otherwise register a roster row with a gym-A member's email and take over the account." | ADR amendment | ADR-0009 §Amendment 2026-07-15 |
| P-119 | `docs/adr/0012-…md` §Decision | 2026-06-30 | "Host→brand: one pure resolver, host-wins precedence (host-map → `?gym=` override if unmapped → `DEFAULT_BRAND`), no environment coupling. Host wins so on a real mapped customer domain the override is structurally inert. Read `host`, never `x-forwarded-host` (equals `host` on Vercel, weaker/spoofable off it)." | ADR decision, verified against vendored Next 16 docs | ADR-0012 §Decision 1 |
| P-120 | same, §Amended 2026-08-28 | 2026-08-28 | "A BYO-domain customer is a second `app='client'` row for the same `gym_id`, not a replacement. Inbound resolution stays N→1. Outbound (invite email, CUENTA preview, legal aviso) must name exactly ONE canonical host per `(gym_id, app)` via `gym_domain.es_principal`; the three outbound DAL selectors order by `es_principal DESC, created_at ASC`." | ADR amendment | ADR-0012 §Amended 2026-08-28 |
| P-121 | `docs/adr/0014-…md` §Decision | 2026-07-02 | "Custom SMTP for auth mail (Resend) is a Phase-3 deliverable, not a launch nicety — Supabase's built-in mailer walls at roughly member #30. PERMANENT CONSTRAINT: Supabase custom SMTP is project-wide, so one shared Supabase project means one sender identity for EVERY gym — auth mail cannot carry a per-gym branded sender (unless a project-per-gym, which ADR-0008 rejects)." | ADR decision (accepted) | ADR-0014 §Decision |
| P-122 | same, §Amendment 2026-07-09 (#75) | 2026-07-09 | "The 'permanent, structural' per-gym constraint held only for project-wide custom SMTP; it did not consider the Send Email Hook (invoked per-message). Now live: per-gym display name on `From:`, per-gym template copy, confirm link minted on the gym's OWN host. What stays true: one sending domain/address (`no-reply@ibookit.lat`); one shared Supabase project unchanged." | ADR amendment | ADR-0014 §Amendment 2026-07-09 |
| P-123 | `docs/adr/0015-…md` §Decision | 2026-07-08 | "The primary join is a bearer token on the row: `clientes.claim_code` — 8-char crypto-random, globally unique, no expiry, single-use (cleared on claim). Email is demoted to contact info; on claim, verified login email overwrites `clientes.email`. The host is never an authz input: an invite opened on the wrong gym's host is hard-redirected to the code's canonical client host; the claim writes membership for the token's gym regardless of where it was opened." | ADR decision, cites 2026-07-08 gap audit (19 confirmed gaps, 31/39 live rows no email) | ADR-0015 §Decision |
| P-124 | same, §Amendment 2026-07-15 | 2026-07-15 | "The invitation link now targets `/activar`, not `/registro` (the `/registro?codigo=` claim arm was later removed 2026-07-24, H2v2 option b). Typed-email match + Turnstile gate the activation. The auth token is minted AND consumed server-side at click time — it never rides the email, so the ~1h link window and mail-scanner prefetch are structurally irrelevant, and the emailed link is as durable as the claim code." | ADR amendment | ADR-0015 §Amendment 2026-07-15 |
| P-125 | `docs/adr/0016-…md` §Decision 1–2 | 2026-08-02 | "Measured 2026-08-02 (#218): all six `auth.sessions` rows `not_after=null`; a session from 2026-07-11 was still refreshing 2026-08-01 (21 days). Revocation is a database trigger on `gym_membership` DELETE (`revocar_sesiones_al_quitar_membresia`), global per identity (not per-gym — `auth.sessions` carries no gym column)." | ADR decision, measured trigger | ADR-0016 §Decision 1–2 |
| P-126 | same, §Decision 3 (`not_after`) | 2026-08-02 | "Time-boxed sessions, inactivity timeout, single-session-per-user are Supabase Auth settings, Pro-plan-and-up only; this project is on free tier — the repo cannot enforce this. Posture recorded for the day the project moves to Pro: time-box 30d, inactivity 14d, single-session off." | ADR decision (posture, not enforced) | ADR-0016 §Decision 3 |
| P-127 | same, §Consequences | 2026-08-02 | "Revocation does not make role removal durable for members. `apps/client/src/app/reservar/page.tsx` calls `reclamarCliente` when membership is missing, so a removed member who signs in again re-mints their own `gym_membership(member)` row on the next page load. The self-heal at `/reservar` is untouched and needs its own decision. Operator/owner removal IS durable (never self-minted)." | ADR consequence (stated, unresolved) | ADR-0016 §Consequences |
| P-128 | same, §Amendment 2026-08-24 | 2026-08-24 | "Trigger: a member was asked for their password after a week away. Live `auth.sessions` showed the old session deleted, not expired — the only mechanism was `signOut()`, whose default is `scope:'global'`: one 'cerrar sesión' tap on any device revoked every device. Fix: all `signOut` call sites now pass `{scope:'local'}` (4 sites). Owner ruling: members must stay logged in across multi-week absences ('the longest period possible') — current free-tier reality (400-day cookie maxAge, refresh tokens never expire server-side, iOS ~30-day website-data eviction) already is the practical maximum; do NOT apply the Pro time-box/inactivity posture to members without a decision that weighs it." | ADR amendment (incident-triggered) | ADR-0016 §Amendment 2026-08-24 |

### Handoff docs (docs/Context)

| id | source | date | claim | stated basis | cite |
|---|---|---|---|---|---|
| P-129 | `docs/Context/2026-08-29-supabase-degradation-jwks-HANDOFF.md` §4 | 2026-08-29 | "`/activar` cross-tenant shield runs as anon → reads no `gym_domain` → never redirects; pre-existing, ticket-worthy." | handoff table entry, state "pre-existing" | §4 table, row "`/activar` cross-tenant shield" |
| P-130 | same, §3 | 2026-08-29 | "Why one slow endpoint freezes every page: every server request runs `supabase.auth.getClaims()` (admin `proxy.ts:78`, `requireOperator`, `activacion.ts:157`, `agenda-miembro.ts:272`, plus client proxy). `@supabase/ssr` builds a new client per request, so auth-js's in-instance JWKS cache never survives → every request re-fetches the JWKS. My forge-demo login landed on `/inicio` after 2min 9s." | measured (live repro) | §3, point 3 |
| P-131 | `docs/Context/2026-08-28-atp404951-unclaim-session.md` §"Learnings worth keeping" | 2026-08-28 | "`/activar` has three rails, not two: fresh-provision, `cuenta_existente`, and the session-bearing `vincular` short-circuit that pre-empts both — `activar/page.tsx:79-88`, if `sesionActiva && codigo && invitacion`, renders `<VincularForm>` (one-click bind, no edge function, no magic link, no `/auth/confirm`). Nulling `auth_user_id` does not log anyone out — only a `gym_membership` DELETE revokes sessions. Any activation test must specify the browser state, not just the data state." | measured (session-derived, live-verified) | §"The three traps" #1, §"Learnings worth keeping" |
