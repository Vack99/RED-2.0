# HANDOFF 2 — spec #222, E · VENTANA endgame (#226 → #229 → #230)

**Session 2 ended 2026-08-03.** Five closed: #223, #224, #225, #227, #228 — all opus-reviewed with a fix round, all on **local main (unpushed)**, gate green at merge (1218/1218). Three remain, serial, all gated. This session's job: land them and close spec #222.

## Where to work

**This session runs in the dedicated worktree `C:\Users\Aaron\Documents\Repos\improving-client-page`** (branch `improving-client-page`, cut from main @ `58889da`, deps + husky already installed). Launch Claude Code THERE, not in `RED-2.0` — the main checkout belongs to the concurrent t3 session. Consequences:

- Integration target is **`improving-client-page`**, not `main`: slice branches ff-merge into it; builders' first step syncs to it (`git merge --ff-only improving-client-page`), NOT to `main`.
- `main` is touched ONCE, at the very end: rebase `improving-client-page` onto `main` (t3 will have moved it), rerun the full gate, then ff `main` — coordinate with the owner if t3 is mid-flight. Still **no push**.

## Boot sequence (do this before anything)

1. `/ponytail` + `/caveman` — **first tool calls of the session.** Main session = ORCHESTRATOR only: reads tickets, briefs agents, judges, merges. Sonnet builders in foreground worktrees, opus reviewers (findings only), fix rounds via SendMessage back to the SAME builder. Fable never staffed.
2. Read `gh issue view 222`, `gh issue view 226 --comments` (the comment carries 2 added obligations), 229, 230, then this file. #188's died-objections table: nothing on it gets re-litigated. Its amendments are still the review axes.
3. **Gate check, before any build:** `gh issue view 203` — #226 builds only after epic #203 CLOSES (owner ruling 2; the RPC sits on the tenant seam). If open, ask via picker (one question, alone) whether to wait or stop. Also `git log --oneline -5` — a concurrent t3 session shipped to main all through session 2; expect main to move under you.

## The graph

```
#226 (db)   RPC + index + denial suite      ← epic #203 CLOSED + scratch creds
#229 (ui)   AÚN A TIEMPO + SIN VENIR badge  ← #226
#230        closeout: perf, rm proto, walk  ← #229 (+ owner HITL)
```

## What already exists (build ON it, don't re-derive)

- **Engine:** `packages/domain/src/lifecycle.ts` — `derivarLifecycle`, `derivarFilaLifecycle` (the ONE row constructor, both tiers consume it), `ordenarLifecycle`, `contarLifecycle` (has `aunATiempo` counts already), `esPorRenovar`, `esPaseSuelto`, `nivelUrgenciaLifecycle`, `RENOVACION_DIAS`, `CUBO_LABEL`. Tile membership + `ausente` + day-16 exit are already computed and tested (~130 tests) — #229 mostly WIRES, it does not derive.
- **CLIENTES:** `apps/admin/src/app/(app)/clientes/_components/clientes-vm.ts` — `tieneCuenta`/visit facts are documented placeholders; conteos type is `Omit<…, "aunATiempo">` ON PURPOSE (compiler blocks a fake zero until #226's data exists). #229 removes the Omit when real facts flow.
- **INICIO:** `…/inicio/_components/inicio.tsx` — POR RENOVAR tile under the `MEMBRESÍAS` header; AÚN A TIEMPO joins it there. Use the `--warning`/`--warning-soft` palette (RED's `--gold` = 1.74:1 on surface — proven unusable). `…/inicio/loading.tsx` must mirror new tiles (guard checks existence only; drift is silent).
- **Cross-seam equality test:** `…/clientes/cross-seam.test.ts` — extend it when #229 adds counts; it is the machine guard for "same word, same number".
- Ruled predicates that must survive review: pendienteOnline = `cuenta_activa && estado === "sin_paquete"` ONLY; fecha wins; a spent pase suelto is never SIN CLASES; sin_paquete floors urgencia to ok.

## #226 spec (db slice — the only migration-bearing one)

Per-cliente last-visit aggregate: **read RPC + serving index** (both existing `asistencias` indexes are partial and mutually exclusive). RPC resolves gym from its parameter with membership checks (no auth-roulette), honors `deleted_at`/`perdonada`, and must source **BOTH** facts: `ultimaVisitaConsumida` (clases clock — `consumio=true` only) AND `ultimaVisita` (any visit — ausente clock). Plus the ride-along from the issue comment: rewrite the seeded Renovación plantilla body (negative días can't render from the `{dias}` token; TS fallback currently prints "-3 días"). Denial suite asserts tenant scoping + the consuming-visit definition on seeded rows; register it in the runner (drift guard forces it). **Green scratch `test:denial` before ff** — `SUPABASE_TARGET_REF=gyyujeguycxxoaqgdnjp SUPABASE_ACCESS_TOKEN=<pat from owner>`; runner refuses the live ref. INICIO budget: ONE new aggregate, no extra roster fetch.

## #229 spec (ui slice)

AÚN A TIEMPO tile (ended 1–15d from expiry, no pase suelto, **no app account**) + `{n}D SIN VENIR` badge on **paid-up members only** (engine computes `ausente` unconditionally; the consumer applies the paid-up rule). Wire #226's facts into `getClientesRoster`/`getRosterResumen` DTOs → drop the vm placeholders + the Omit. Zero-count = "todo al día", no deep-link. Tile deep-links pre-filtered (new filter param, same predicate both screens — extend cross-seam test). Skeletons updated.

## #230 spec (closeout)

Perf gate: INICIO + CLIENTES under 50ms with margin — **the spec conditioned `getRosterResumen`'s full-roster walk (shipped in #225) on exactly this run**; if it fails, that read is what to fix. `rm -rf apps/admin/src/app/proto` (+ the proxy auth bypass if still present) — deletes the last `resumirRoster` callers; retire it. Rename `PaseClienteDTO.porVencer` → renovar naming (`asistencia.tsx` "· VENCE 3D" chip). Then owner walk + IMPI Marcanet lookup (classes 9/42) — HITL, not agent work.

## Landmines (all burned at least once)

- Supabase MCP is bound to LIVE PROD (`apply_migration` hits production). Migrations go through `supabase/migrations/` + scratch. Never `supabase link`/`db push` (56/78 filenames unrecognized → re-applies seeds).
- **NEVER `git push`** — owner consent per push, none granted. Commit + ff local main only.
- Concurrent-session races: worktree agents can branch off a STALE base — every builder's first step is `git merge --ff-only improving-client-page` (or rebase onto it) and verify the files it depends on exist. An ff-merge may abort or find the target moved: rebase-in-worktree + retry. Bash and PowerShell share a persisted cwd — a `cd` in one moves the other; use absolute paths for git state checks.
- Husky v9: never run `husky` with an argument. Fixtures must include finite class packs + pendienteOnline rows + a forge-shaped mix.
- t3 session's #173 commit (`cfc66ad`) carries migration `20260803120000_mi_membresia_visit_count.sql` — confirm its scratch `test:denial` ran before the next consented push.

## Loose ends (file as ordinary issues after ONE owner nod via picker)

Phone-arm digit normalization on roster searches — includes the pre-existing `vender.tsx` picker bug (letters-only query → tel arm matches every client with a phone; needs a digit-presence guard, `telDigits` already exported from `@gym/format`). · 1001-row truncation (resumen unordered vs roster ordered → two screens truncate different windows). · CLIENTES default-order affordance (ruled order is unlabeled when no sort lit). · #184's `router.refresh()` gap. · `∞ cl` backport.

Owner TODOs riding along: #205/#206/#207 (epic 203 HITL), commit the docs/CLAUDE.md dirt (this file rides that commit), keep `2026-08-02-186-ui-imitation-ip-research.md` OUT of the public repo.
