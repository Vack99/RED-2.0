# Brand-structure session — HANDOFF (2026-08-10 → next morning)

Session protocol: /caveman /ponytail /keep-it-lean · orchestration session (sonnet research
agents, foreground; taste deliverables written in the main session, not delegated).

## Where we are

Process doc = `docs/brand/BRAND-PLAN.md` — phases A→D, gates, rules. Read it first; it
supersedes the old Downloads/BRAND_HANDOFF.md draft.

| Phase | State |
|---|---|
| A — Inventory | DONE, folded into plan §A (33-key token contract, BrandModule slots) |
| CP1 | PASSED — rulings in plan §A: clean slate; minimal-modern-clean-warm; **recessive = the differentiator**; nothing flashy |
| B — Strategy | DONE + owner-approved w/ corrections → `docs/brand/01-strategy.md` |
| C — Identity direction | DONE (gateless) → `docs/brand/02-identity-direction.md` |
| Niche sweep | DONE → `docs/Context/2026-08-10-sales-niche-sweep.md` (8 new STRONG; piano/private lessons = capacity-1 PARTIAL, sellable; feeds marketing campaign later) |
| **C2 — Visual execution** | **NEXT** |
| D — Fable prompt | after CP3 |

All committed on `main` through `cfd2046`, **unpushed** (push = owner-gated, batches ride the
next consented push).

## Next action: Phase C2

Generate candidates for: wordmark + mark, app icon (flat SVG, renders with NO stylesheet),
favicon set 16/32/180/192/512, OG template, final palette + type — **chosen together, tested
at favicon size and on dark**. Tools: `brandkit` skill / image generation; opus for asset
agents if delegated; escalate to human designer only if output misses the bar. Then 🛑 CP3:
owner picks.

Binding constraints (full detail in 01/02 docs — don't re-derive):

- Name `iBookit` capital B, domain `ibooki.lat` (final-t drop is deliberate, never "correct"
  either). Wordmark must survive phonetic Spanish reading; the i/B seam must stay legible.
- Vertical-neutral: the three-businesses test (piano school + climbing gym + dance academy).
  No gym iconography.
- Register: warm, airy, serious-softened-by-warmth. Neutral-dominant, ONE scarce accent, away
  from green/red/warning (semantic states). Not blue/teal SaaS, not athletic black/red.
- Type: one humanist variable sans, open license, tabular numerals, full es-MX diacritics.
- Two expressions: corporate vs house theme (`base` module stays quieter than corporate).
- Anti-patterns list in 02 §last — includes "no big-impact identity" (CP1).

## After the structure

Build order (owner-stated): marketing/commercial page FIRST, own-brand admin+client frontend
LAST. Neither starts until D is done.
