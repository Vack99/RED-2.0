# HANDOFF — spec #222, E · VENTANA execution

**Session ended 2026-08-03.** Map #180 CLOSED, #188 cross-exam CLOSED, spec **#222** published, tickets **#223–#230** live, all `ready-for-agent`. **Zero product code shipped yet.** Next session's job: close as many of the 8 as the gates allow, by orchestration.

## Boot sequence (do this before anything)

1. `/ponytail` + `/caveman` — **first tool calls of the session.** The main session is an ORCHESTRATOR: it reads tickets, briefs agents, judges output, merges. Every token it spends narrating or implementing inline is the usage failure that ended the last two sessions. Caveman cuts main-session prose ~75%; ponytail kills scope creep in briefs and reviews. Subagent prompts are unaffected — write those complete.
2. Read `gh issue view 222`, then this file. Do **not** re-read #180–#188 unless a specific claim needs its citation — #188's comment carries a **died-objections table**; nothing on it gets re-litigated, ever.
3. `git status` — the repo sits on `epic-203-tenant-in-effect`, DIRTY (modified CLAUDE.md, supabase/tests/*.sql, untracked docs). That is #203's work. **Touch nothing on it. All VENTANA slices branch off `main` in fresh worktrees.**

## The graph, and what runs in parallel

```
WAVE 1 (parallel, independent files):   #223 engine (domain)   ∥   #224 diacritic search
WAVE 2 (serial, wide blast radius):     #225 seven-engines→one          ← #223
WAVE 3 (serial, same app surface):      #227 CLIENTES list              ← #225
                                        #228 INICIO renovar tile        ← #225 + #227
GATED (external):                       #226 RPC+index+denial suite     ← epic #203 LANDING
                                        #229 AÚN A TIEMPO + badge       ← #226 + #228
                                        #230 closeout (perf, rm proto, owner walk) ← #228 + #229
```

- **Wave 1 is the only true parallel pair.** Run both in **worktree isolation** (they share zero files if the fold util lands in the domain/format tier — brief #224's agent to put it there, not in the component).
- #225 is deliberately ONE slice (two live meanings of a band IS the bug). Do not split it to parallelize.
- #227 → #228 are serial by dependency and by surface (same app, #228 consumes #227's filter param).
- **Realistic ceiling without #203: five closed (#223, #224, #225, #227, #228).** That is a good session. #226/#229/#230 stay gated — do not "unblock" them by building the RPC on the moving tenant seam; the owner ruled the sequence.

## Orchestration playbook

- **One agent per ticket, fresh context each, FOREGROUND** (owner standing rule — background reads as hiding work). No wide fan-outs; the pipeline above is the whole shape.
- **Models:** implementation agents = **sonnet** with sharp, complete briefs (each ticket body IS the spec — paste it plus the relevant #222 decisions into the brief). Escalation ladder on weak output: re-brief same tier → opus. **Reviews = opus.** Fable: never staffed.
- **Review cadence:** after each wave, one opus review agent per merged slice (`/code-review` axes: Standards + Spec-vs-#222). Cheaper than re-opening a shipped defect. The #188 amendments are the review checklist — especially: fecha precedence, pendienteOnline never below the dead, VIGENTE = one number on both screens, no person-verdict nouns anywhere (header, export, badge).
- **TDD is native here:** #223's ACs enumerate the #188 edge cases as named tests; #225's ACs name the ~18 pinned assertions to rewrite (deliberate — they encode the OLD bands; the flip-list in #188 is the checklist). An agent "fixing" a red band-test by reverting the band has failed the ticket.
- **Merge discipline:** slice branch → green pre-commit gate (`pnpm lint && typecheck && test`) → fast-forward local `main`. **NEVER `git push`** — owner consent per push, none granted. Batch everything local.
- **Fixture debt is an AC, not a nice-to-have:** #223's test fixtures MUST include finite class packs, pendienteOnline rows, and a forge-shaped mix — the two blind spots that hid the proto's worst defects.

## Gates only the owner can open (ask via picker, one question, never under findings)

1. **#203 landing** — 7/17 on the dirty unpushed branch, `test:denial` unrun. Unlocks #226→#229→#230. Its completion is its own session's scope; do not absorb it silently.
2. **Scratch creds for #226's `test:denial`** — `SUPABASE_TARGET_REF=<scratch>` + `SUPABASE_ACCESS_TOKEN`. Scratch project `gyyujeguycxxoaqgdnjp` was kept as the test bed. Runner refuses the live ref.
3. **Push/deploy** consent.
4. **#230's HITL**: owner walk + the 5-minute IMPI Marcanet lookup (classes 9/42).

## Landmines (all proven, all cited in #188 — don't re-derive)

- **Supabase MCP is bound to LIVE PROD.** Read-only queries only; `apply_migration` hits production. Migrations go through `supabase/migrations/` + the scratch convention.
- Never `supabase link`/`db push` to prod (56/78 filenames unrecognized → would re-apply seeds).
- Husky v9: never run `husky` with an argument.
- The client app already renders lapsed correctly (`vencido`, date-first) — it is the PRECEDENT for #223's fecha precedence, not a surface to change.
- `/proto` is reference material for #227/#228 (the walked shape + `lifecycle.ts` predicate impl) until #230 deletes it. Its fixture is blind (all-ilimitado, no pendienteOnline) — copy its shape, never its data.
- Loading skeletons are ACs on #227/#228 — the guard checks existence only, so drift is silent.

## Loose ends riding along

- File as ordinary issues if the owner nods: #184's `router.refresh()` gap, 1001-row silent truncation, `∞ cl` backport.
- Owner TODO: commit `docs/Context/2026-08-01-181-…md` (independent-creation timestamp); keep `2026-08-02-186-ui-imitation-ip-research.md` OUT of the public repo permanently.
- This handoff file is uncommitted on a dirty #203 branch — leave it; it rides with the next docs commit on main.
