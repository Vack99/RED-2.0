> **Phase-3 kickoff prompt.** Paste the fenced block into a FRESH session on this repo (zero prior context). It runs the house pipeline (`grill-with-docs → to-prd → to-issues → to-goal`) to emit Phase-3's PRD + sliced issues + goal file, then STOPS for review. A later fresh session pastes the generated `docs/prompts/goal-platform-phase3-rls.md` to execute the slices (as Phases 1–2 did). DRY against the docs — it points, it does not restate; only the VERBATIM blocks are load-bearing text. This is the FIRST of the two planning sessions: the Phase-4 (brand system) planning session starts only AFTER this one publishes its issues (Phase 4 then consumes the gym columns you define + cites your gym-schema issue number). The two EXECUTE in parallel — the COORDINATION section is the shared contract both kickoffs cite; honour it exactly.

```
PHASE 3 — "Tenant/identity foundation (data track)" of the RED-2.0 multi-gym platform. Fresh session, zero prior context. THIS session: run the house pipeline (grill-with-docs → to-prd → to-issues → to-goal) to emit the planning artifacts, then STOP for review. You are NOT implementing slices.

RECONCILIATION (the one thing to get right): Phase 3 migrates the LIVE single-tenant Forge admin onto a gym-scoped multi-tenant spine — gym / gym_domain / gym_membership / member-evolution + RLS-by-membership replacing the 22 per-auth.uid() policies — ships member self-register + claim-by-match, swaps HOST_TO_BRAND for DB-backed host→gym resolution, stands up custom SMTP and per-gym timezone, and executes the folio/user_id re-key contract. Forge stays green at EVERY commit (expand/contract, live DB). This is a DEPTH phase, not a tracer: its schema/policy/helper structure is demanded by the acceptance criteria — see KEEP-IT-LEAN WIRING. What is NOT here: catalog/scheduling tables (Phase 5), booking/subscription/payments/abuse/notifications and the RED-designed auth screens (Phase 6), ALL brand-render work — base module, token-override merge, brand.css from row data, admin de-brand (Phase 4, planned after this session, executed in parallel), observability (Phase 7). Current state: live Supabase hjppxawglmukfvsgmcog, one operator, 7 domain tables, 18 migrations; apps/{admin,client} + packages/{domain,format,data,ui,brand} on main @ ef05c4c+; apps/client is the Phase-2 skeleton — no registro/entrar UI exists; clientes never authenticate today. Confirm the tree and migrations with your own eyes.

READ FIRST (load-bearing; follow their own cross-links, don't expect me to duplicate them):
- docs/planning/2026-06-29-target-data-model-and-decisions.md — THE shield: identity (§1), claim-by-match (§2), three RLS classes (§3), target schema + folio/user_id re-key contract (§4); you DEFINE the gym.brand_module_id + token_overrides columns here for Phase 4 to consume, invariants (§5), parked defaults (§6). Honour, don't restate.
- docs/planning/2026-06-29-multi-gym-platform-roadmap.md — Phase-3 row (deliverable + full exit criteria incl. HOST_TO_BRAND deletion, SMTP, timezone), §5 guardrails, foundation-first sequencing.
- docs/adr/0009-…two-tier-auth-member-claim.md — the identity spine + bounded veto + denial-test-first cutover. docs/adr/0008-… — the hinge: RLS-by-membership is the boundary, the proxy host NEVER is. docs/adr/0012-… — §5 declares HOST_TO_BRAND a Phase-3 stub; Forward-looking specifies the async swap + keyspace split + the gym-column interface you define here for Phase 4. docs/adr/0001-… — proxy.ts Node-only, server-only DAL, no ORM, getClaims() never getSession(). All SETTLED — cite, don't re-mint.
- docs/adr/0006-… — needs an AMENDMENT: its "Supabase only sends auth mail" assumption is wrong (audit finding 3); it also names Resend.
- docs/superpowers/audits/2026-07-01-multitenant-branding-scale-audit.md — this phase owns findings 1, 3, 4, 5, 6, 9, 10 + the verified "Explicitly do NOT fix" traps.
- docs/superpowers/specs/2026-06-29-monorepo-member-platform-tenancy-design.md — mine §6 (SECURITY DEFINER helpers, initplan caching) and §9 (live-migration steps), BUT its §5 `staff` table and §7 invite-only onboarding are SUPERSEDED by ADR-0009 — re-express its helper SQL against gym_membership, never copy it.
- CONTEXT.md ("Plataforma multi-inquilino" section — extend, don't re-invent) · docs/health/accepted-debt.md (L-001..L-003 @ threshold 900 — the finding-5 ledger the grill rules on).
- Code anchors: supabase/migrations/20260531211105 (registrar_venta + toggle_pase ×2 hardcoded Chihuahua), 20260530023224 (venta_folio_seq, ventas_folio_uq, the auth.uid() policy pattern), 20260531210400 (rls_auto_enable — stays ON), supabase/tests/rls_cross_tenant_denial.sql (manual-run, prod UUIDs — finding 6's start), packages/format/src/fecha.ts:13 (TZ + ~18 call sites), packages/brand/src/{host-map.ts,resolve-brand-id.ts,brand-id.ts}, apps/{admin,client}/src/proxy.ts, docs/runbooks/hitl-16-vercel-deploy-verify.md (the HITL gate pattern to clone; finding 15 region co-location is already recorded there, step 3 + checklist — verify, don't redo).
Next 16 has breaking changes vs your training data — read node_modules/next/dist/docs/ before specifying any Next code. AGENTS.md carries the Husky-v9 caveat.

PIPELINE (run in order, quiz me at each checkpoint; exact skill names):
1. /grill-with-docs — lock the OPEN decisions against the shield/ADRs, updating CONTEXT.md inline (Phase-3-owned rows only — see COORDINATION) and authoring ADR-0013 — Gym-scoped RLS mechanism (audit finding 9): initplan-cached SECURITY DEFINER membership helpers re-minted against gym_membership (the spec's is_staff_of/has_role target the dead `staff` table), search_path='' + EXECUTE-revoke posture, the standard gym-scoping predicate every policy uses; record JWT custom-claims as rejected-for-now (audit-verified trap — don't relitigate) but the documented scale path. SMTP: author ADR-0014 — Custom SMTP (it clears the ADR bar per audit finding 3 — real vendor tradeoff, permanent one-platform-sender constraint, and ADR-0006 misleads) PLUS the ADR-0006 amendment killing its "Supabase only sends auth mail" clause. Claim mechanics: an ADR-0009 AMENDMENT only if something surprising locks (e.g. verified-email-gated claims), not a new number. Also lock: (a) pre-auth readability — anon-read policy on gym_domain + safe gym subset vs privileged server client for the proxy lookup; (b) resolver mechanics — async signature returning tenant + derived brand, PLACEMENT (the cruiser edge brand✗→data/domain makes a DB lookup inside @gym/brand ILLEGAL — inject the lookup or resolve app-side/@gym/data; mandatory grill question), unknown-host, ?gym= semantics for an open slug set, v1 cache posture (Edge Config is later); (c) claim-by-match execution — EXERCISE THE VETO with me first (it expires at Phase-3 start), then mechanism (atomic SECURITY DEFINER RPC vs auth hook vs server action), verified-email predicate, ambiguous-match behavior; (d) SMTP provider/sender/templates + the one-platform-sender constraint; (e) the minimal self-register/claim vehicle (exit says "can self-register" — invocable end-to-end, but RED's designed screens are Phase 6); (f) clientes→member shape — additive columns vs rename (expand/contract favors additive; CONTEXT.md vocabulary rides on it) + whether the membership row writes in the same atomic step; (g) perfil→gym column split + cobro re-key semantics (CLABE = owner-only secret) + the lone-operator backfill row; (h) timezone threading — per-call tz arg vs request-scoped context, toggle_pase's source of gym.timezone, honest renames of *Chihuahua helpers; (i) denial-suite vehicle — Supabase preview branch (MCP create_branch) + seeded fixtures vs local pgTAP (no local Docker today), and where it gates; (j) unpaged clientes readers — ride Phase 3 vs stay trigger-gated debt (triggers recalibrated to 900 on 2026-07-01, uncrossed); (k) canonical legal name per gym (HITL fact — shield §6 says confirm at Phase 3). Everything else is SETTLED — cite, don't re-mint. Do NOT run brainstorming — a "missing" decision is in the shield or a 🅿️ default.
2. /to-prd — publish the Phase-3 PRD to the tracker + mirror to docs/prds/prd-tenant-rls.md (PINNED filename). MUST embed the "### Design principles" section — the canonical keep-it-lean clause (copied from the skill at assembly time; see KEEP-IT-LEAN WIRING) immediately followed by the NAMED PRESENT-NEED EXCEPTIONS paragraph below VERBATIM — at the end of ## Implementation Decisions. Carry a suggested S0..Sn decomposition + per-slice skills + docs-guard timing as guidance (next stage owns the final cut). Re-derive the docs-guard-timing rule for any new paths so tools/guards/docs.test.ts stays green.
3. /to-issues — thin vertical slices, Blocked-by DAG, denial-test-BEFORE-policy inside every RLS slice. The gym + gym_domain schema slice (creating the brand_module_id + token_overrides columns Phase 4 consumes, and seeding forge/red rows from HOST_TO_BRAND's 5 entries — note the RED-admin host is NOT among them; its gym_domain row is a later human insert, not your scope) goes FIRST in the DAG. Because Phase 4 planning starts only after your issues publish, its merge slice (IF its grill needs the real table) cites this slice's real issue number directly (see COORDINATION) — no mid-flight handshake. Initiative label: platform-phase3-rls-2026-07 (gh label create; never reuse another initiative's label — the orchestrator's per-turn label fetch would sweep it). Mark `hitl` (+ initiative label, NEVER ready-for-agent): the SMTP standing-up slice and the terminal live cutover + deploy-verify gate (runbook clone of hitl-16). Everything else AFK ready-for-agent.
4. /to-goal — emit docs/prompts/goal-platform-phase3-rls.md (PINNED; K=3, cap = ceil(slices/K)+1). Insert, phase-local into each slice's Gate 1 (Elegance) blockquote (AFTER "…minimum diff needed to satisfy the acceptance criteria." and BEFORE "Return `YES` or `NO`."), the canonical clause (copied from the skill) followed by the GATE-1 DEPTH ADDENDUM below VERBATIM. Do NOT edit the global gate-prompts.md — phase-local trial only. RECOMMEND the fresh-eyes opt-in gate model (gate-prompts.md) for the WHOLE initiative — live-DB tenant isolation is exactly the high-stakes case it exists for, and gate model is a single per-initiative choice (no per-slice hybrid mechanics exist; the terminal cutover slice is hitl anyway). List keep-it-lean as an ALWAYS per-slice skill. Solo-main workflow: slices on stacked branches, no PRs, gate diff = BASE_REF..HEAD; I fast-forward to main after.
Then STOP; summarize ALL artifacts — the grill's doc commits (ADR-0013, ADR-0014, the ADR-0006 amendment, CONTEXT.md rows) plus the PRD, issues, and goal file. Do not execute slices.

ANTI-CREEP DELTAS (the non-obvious traps you'd still get wrong — the fence is "ONLY the enumerated tables/policies/helpers", not "no schema"):
- ONLY the four spine tables + member-evolution columns. NO catalog/scheduling entities (class_*, schedule_template, plan/plan_feature, room, gym_hours, gym content — Phase 5), NO booking/subscription entities (reservation, subscription, waitlist, contact_message — Phase 6), no asistencias→reservation evolution.
- ventas/paquetes are touched ONLY for the folio + user_id re-key contract — the payment/plan entity evolutions ride Phases 5/6.
- ZERO brand-render work: base module, zod merge, brand.css serialization, admin-shell de-brand are Phase 4's — Phase 3 only delivers the gym row + resolution Phase 4 consumes. Do not "helpfully" edit packages/brand/src/{brand-id,registry}.ts or either layout.tsx (Phase-4-owned; see COORDINATION).
- NO designed auth screens, no abuse posture (Turnstile/per-IP), no payments — Phase 6.
- NO audit-refuted capacity work, incl.: JWT-claims RLS, pooling, sharding/replicas, caching layers, Edge/multi-region, Edge Config, dynamic() per-brand imports, Supabase MAU tiering, per-gym projects, parametrizing es-MX/MXN/+52 — the audit's "Explicitly do NOT fix" list is the full fence.
- NO roles beyond owner|operator|member (spec's coach/secretary vocabulary is Phase 5; its staff TABLE is superseded outright). No self-serve onboarding, no BYO-domain queue.
- rls_auto_enable stays ON; every new tenant table gets gym_id + RLS + a gym_id index. gym_id on registration is server-authoritative from the resolved host — never client-supplied.
- Keep all shields green per commit (boundary, catalog, server-only, lint+typecheck+test) and CONTEXT.md/ARCHITECTURE.md in sync per the docs-guard timing rule.

ACCEPTANCE (observable, not asserted — beyond what the docs already say):
- The denial suite is ONE repeatable command against a SEEDED Supabase preview branch (zero hardcoded prod UUIDs), covering cross-gym read/write, member-vs-member, member-vs-operator-surface, non-owner-vs-cobro/CLABE, and registrar_venta/toggle_pase cross-gym vectors — recorded green BEFORE the policy cutover and re-run green AFTER the per-auth.uid() drop.
- Synthetic gym #2 probe on the SEEDED preview branch (same vehicle as the denial suite — grill item (i)) with a non-Chihuahua IANA zone (e.g. America/Mexico_City): self-register → claim a pre-seeded row → gym-scoped read, plus one sale — OBSERVED: folios sequence independently per gym and dates render in gym #2's zone; gym #1 Forge regression vehicle = existing Forge test suite green + the deploy-verify runbook checklist unchanged. This makes riskiest-assumption #2 falsifiable.
- HOST_TO_BRAND deletion is grep-proven — zero references in code + living docs (Phase 3 owns the minimal ARCHITECTURE.md scrub; archival records — the Phase-2 PRD/goal/runbooks — are exempt, see COORDINATION) — AND the live deployed hosts resolve from gym_domain rows.
- Auth mail: signup-confirmation + reset received on a real inbox FROM the custom sending domain (headers recorded), links clicked, session established.

HITL / HUMAN-ONLY (I do these; the orchestrator must not): exercise the claim-by-match veto (at the grill checkpoint, before any identity code); confirm canonical legal name + timezone + contact facts per gym; SMTP vendor signup, DNS (SPF/DKIM/DMARC), credentials into the Supabase dashboard, template + rate-limit edits; live deliverability verification on a real inbox; approve the DESTRUCTIVE cutover steps on the live prod DB (dropping per-auth.uid() policies + redundant columns) after the denial suite is green; run the terminal deploy-verify runbook (register→claim→scoped-read live, Forge admin ops in prod) + provision any hosts it needs; relay the gym-schema issue number to the Phase-4 session; fast-forward the shipped stack to main.

PER-SLICE SKILLS (the goal file wires these in): superpowers:writing-plans (roadmap-mandated per phase, shield as spec); superpowers:test-driven-development — non-negotiable: denial test written BEFORE the policy it guards, per slice; supabase-postgres-best-practices-RED on EVERY migration/policy/RPC touch (initplan caching, search_path='', index-every-gym_id); Supabase MCP branch tools (create_branch/execute_sql/get_advisors) for the mechanized denial suite + advisor checks after each policy migration; improve-database-architecture as the post-cutover exit audit; keep-it-lean (with the depth framing below); superpowers:verification-before-completion + verify — drive the real flows, not just tests; codex-implementation for bulk mechanical work (the ~18-call-site tz threading, constraint re-keys — gpt-5.5 territory per CLAUDE.md; verify the skill is installed before the goal run, else fall back to raw `codex exec`). Do NOT invoke (cargo-cult here): superpowers:brainstorming, sector-map, improve-codebase-architecture (mid-migration refactor bait), to-map/to-findings/to-health (ledger just recalibrated — the grill rules on finding 5), grill-me (use grill-with-docs), prototype/frontend-design/dataviz (data track — the minimal registro vehicle is deliberately unstyled).

COORDINATION (Phase-4 planning starts AFTER this session publishes its issues; the two EXECUTE in parallel. Both kickoffs cite this same contract):
- ADR numbers: Phase 3 = ADR-0013 (gym-scoped RLS mechanism) + ADR-0014 (custom SMTP) + the ADR-0006 amendment. Phase 4 (planned next) takes ADR-0015 only if a grill lock CONTRADICTS ADR-0012's text (default: a later "Amended: (Phase 4)" pass on ADR-0012). You author + may amend ADR-0012 here; Phase 4 only cites it.
- Initiative labels: platform-phase3-rls-2026-07 (yours) vs platform-phase4-brand-2026-07 (theirs). No slice ever carries both — each orchestrator's per-turn `--label` fetch would sweep the other's queue. prd/ready-for-agent/hitl stay shared cross-cutting labels.
- Pinned filenames: docs/prds/prd-tenant-rls.md + docs/prompts/goal-platform-phase3-rls.md (Phase 4: prd-brand-system.md + goal-platform-phase4-brand.md).
- CONTEXT.md ownership (append-only-disjoint): Phase 3 owns the tenancy/identity/resolution rows — 'inquilino' (update to the real gym row), the 'host→inquilino→marca' chain row, the 'Flagged ambiguity' paragraph, + NEW rows it mints (gym_domain, gym_membership/membresía, member/claim-by-match, RLS-por-membresía). Phase 4 owns 'marca', 'contrato de marca', 'módulo de marca', 'A escala', + its new rows. You commit your tenancy rows first; Phase 4 extends the file with its brand rows afterward. Edit only your rows.
- Execution seam ownership: Phase 3 OWNS packages/brand/src/resolve-brand-id.ts (sync→async swap; DB lookup must live app-side/@gym/data — the brand-is-presentation-only cruiser edge is FROZEN for both phases), host-map.ts (deletes it), BOTH proxies' resolution edits (rebase hotspot — Phase 4 never edits a proxy), comment-only cruiser edits, and the minimal HOST_TO_BRAND scrub of ARCHITECTURE.md. Phase 4 OWNS brand-id.ts (BrandId keyspace incl. 'base' + DEFAULT_BRAND), registry.ts + base module + zod merge, and BOTH layouts (rebase hotspot — Phase 3 never edits a layout). Phase 3 treats BrandId as an opaque registry key; its open tenant-slug type lives in the data tier. Historical records (docs/prds/prd-multitenant-tracer.md, docs/prompts/goal-multitenant-tracer.md, docs/runbooks/*) are ARCHIVAL — neither phase rewrites them.
- SPLIT files (owned by block, not whole): packages/brand/src/brand.test.ts + index.ts. Phase 3 owns removing the HOST_TO_BRAND import (test line 4), the HOST_TO_BRAND describe block (lines 35–46), and index.ts's HOST_TO_BRAND export (line 6); Phase 4 owns the "@gym/brand registry" describe block (census line 13, DEFAULT_BRAND assertions lines 16–19) and any new index.ts exports. The blocks are disjoint — pull-rebase resolves any race.
- Pinned header contract (what keeps the hotspot files commutative): the proxy stamps x-gym = the resolved tenant id/slug (NEW, Phase 3 adds it; presentation/UX only, NEVER authz — ADR-0008 hinge) and x-brand = a registry key (today stamped from the static host-map; = the gym row's brand_module_id once Phase 3's DB lookup lands), ALWAYS validated in the layout via Object.hasOwn with DEFAULT_BRAND fallback — DEFAULT_BRAND's value is Phase 4's grill call, the ONE fallback knob. Phase 3 stamps; Phase 4's layouts read both and never re-resolve. Quote this in the goal file.
- Gym-row column interface (Phase 3 DEFINES here, Phase 4 CONSUMES): your gym migration creates exactly `brand_module_id text NOT NULL` (opaque registry key; NO FK/CHECK and NO pinned DB default — render-side validation + the default/fallback module id are Phase 4's grill decisions; treat the value as opaque) and `token_overrides jsonb NOT NULL DEFAULT '{}'::jsonb` (the jsonb VALUE shape is Phase 4's zod decision; treat it as opaque), and seeds forge/red rows with brand_module_id 'forge'/'red' + '{}' overrides. Phase 4 never writes a migration against gym.
- AT MOST ONE cross-initiative edge: Phase 4's "base module + gym-row override merge" slice is Blocked-by THIS phase's gym+gym_domain schema slice IF (and only if) Phase 4's grill decision (f) locks a demo needing the real table; if it locks the fixture-through-the-real-merge-path demo, there is NO edge. Schedule the schema slice FIRST regardless (Phase 4 puts its merge slice LAST, so any blocking window is near zero). Issue-number handshake is trivial: your issues publish before Phase 4 planning starts, so its merge slice cites your real gym-schema issue number directly — no placeholder, no relay. Phase 4's goal run will see the edge as an expected external Orphan. Merge order to main: your gym-schema slice before their merge slice; everything else in either stack is order-free. RED-admin go-live is NOT a second edge: its gym_domain row is a later human insert (Phase-4 HITL, post-swap).

KEEP-IT-LEAN WIRING (depth-phase framing — read carefully): invoke /keep-it-lean; its ONE canonical clause is the blockquote in its "Wiring it into the pipeline" section — COPY IT FROM THE SKILL at assembly time (single source; if any copy and the skill ever differ, the skill file wins). Do not paraphrase it, and do not add any "thin tracer / prefer duplication" preamble — that was Phase-2 framing and it is WRONG here: Phase 3's acceptance criteria REQUIRE structure; the clause guards only against additions BEYOND the enumerated present needs. Assembly: (a) to-prd writes the "### Design principles" section = the canonical clause (from the skill) immediately followed by the NAMED PRESENT-NEED EXCEPTIONS paragraph below verbatim; (b) to-goal inserts, phase-local into each slice's Gate 1 (Elegance) blockquote (after "…minimum diff needed to satisfy the acceptance criteria.", before "Return `YES` or `NO`."), the canonical clause followed by the GATE-1 DEPTH ADDENDUM below verbatim. NEVER edit the global to-goal/gate-prompts.md.

------------------------------------------------------------------------------------------------
VERBATIM — NAMED PRESENT-NEED EXCEPTIONS paragraph (PRD only, directly beneath the canonical clause):

**Named present-need exceptions.** Phase 3 is a depth phase: its acceptance criteria REQUIRE the following structures, each with an enforced boundary or in-phase consumers, and they PASS the principles above by construction: (1) the four spine tables — gym (incl. the pinned brand_module_id + token_overrides columns), gym_domain, gym_membership, member-evolution columns; (2) gym-scoped RLS policies on every tenant table — a policy per table is the deliverable, not duplication to prefer away; (3) the SECURITY DEFINER membership-helper functions — consumed by every table's policies, so single-caller rejection does not apply; (4) the mechanized denial-suite fixtures/harness — the phase's highest-risk surface demands it; (5) tz-parameterization of @gym/format (~18 call sites) and the toggle_pase RPC; (6) the per-gym folio counter row + registrar_venta rewiring + the four user_id-keyed unique re-keys; (7) async host→gym resolution replacing HOST_TO_BRAND (lookup app-side/data-tier; pure derivation stays in @gym/brand). Structure BEYOND this list remains rejected.

------------------------------------------------------------------------------------------------
VERBATIM — GATE-1 DEPTH ADDENDUM (goal file only, appended directly after the canonical clause inside the Gate 1 blockquote; do NOT edit the global gate-prompts.md):

This is a depth phase, not a tracer. The PRD's Named present-need exceptions — the four spine tables, per-table gym-scoped RLS policies, the many-consumer SECURITY DEFINER membership helpers, the seeded denial-suite harness, tz-parameterization, the per-gym folio counter, and the async host→gym resolver — are acceptance-criteria-required structure and PASS this gate. Reject only structure BEYOND that list: any generic multi-tenancy framework, policy-builder DSL, roles beyond owner|operator|member, tables/columns/indexes no acceptance criterion exercises, per-tenant config surfaces, or caching layers.
------------------------------------------------------------------------------------------------
```

After paste: the session should confirm the tree, run the four skills in order pausing at each checkpoint (exercising the claim-by-match veto at the grill), then stop with a summary of all artifacts. Phase 4 planning follows once these issues publish; execution then happens in a later session off the emitted goal file, in parallel with Phase 4 under the coordination contract above.