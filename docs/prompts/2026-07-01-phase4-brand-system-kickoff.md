> **Phase-4 kickoff prompt (frontend track — the SECOND planning session; start it only AFTER Phase 3 has published its issues).** Paste the fenced block into a FRESH session on this repo (zero prior context). It runs the house pipeline (`grill-with-docs → to-prd → to-issues → to-goal`) to emit Phase-4's PRD + sliced issues + goal file, then STOPS for review. A later fresh session pastes the generated `docs/prompts/goal-platform-phase4-brand.md` to execute the slices. DRY against the docs — it points, it does not restate; only the VERBATIM block is load-bearing text. By the time you start, Phase 3 has already defined the gym columns you consume and published the gym-schema issue you cite; the two EXECUTE in parallel under the SAME coordination contract.

```
PHASE 4 — "Brand system (full brand modules)" of the RED-2.0 multi-gym platform. Fresh session, zero prior context. THIS session: run the house pipeline (grill-with-docs → to-prd → to-issues → to-goal) to emit the planning artifacts, then STOP for review. You are NOT implementing slices. Phase 3 (tenant/RLS) planning has already completed through to-issues on this same repo — it defined the gym-row columns you consume and published the gym-schema issue you cite. The two EXECUTE in parallel; the COORDINATION section below is a first-class requirement, not advice.

RECONCILIATION (the one thing to get right): Phase 4 is a DEPTH phase, not a tracer. It builds the full @gym/brand system — brand-keyed CSS-var token sets generalising both apps' globals.css, logo components, animation modules (forge-* login sequence extracted + the extant RED ignition), copy, a neutral `base` module, and a zod-validated gym-row token-override merge (the brand-is-DATA path) — and de-brands the admin shell's 4 Forge hardcodes, so EITHER app renders fully in either brand by host, no FOUC, prefers-reduced-motion respected, and a gym with NO dedicated code module renders from base + row data. NOT this phase: the 12-screen RED client app (Phase 6); any schema/migration/RLS/gym-table/member work, HOST_TO_BRAND retirement, the sync→async resolver swap, proxy edits (ALL Phase 3 — host-map.ts and resolve-brand-id.ts are READ-ONLY for this phase: their current static/sync shape is Phase-2 state that Phase 3 retires at execution; depend only on the pinned x-brand header, never on the resolver's signature or the host-map's existence); dynamic() per-brand code-splitting / Edge work (Phase 7, audit-parked); catalog/booking schema (Phase 5/6). Repo today: packages/brand/src = {brand-id,host-map,resolve-brand-id,registry}.ts + forge/{tokens,logo} + red/{tokens,logo,login-animation}; brand.test.ts PINS an exactly-two-brand census (deliberate tripwire the base module consciously updates); both layouts already inject brands[brandId].css via <style dangerouslySetInnerHTML> per ADR-0012 §3. Confirm the tree with your own eyes.

READ FIRST (load-bearing; follow their own cross-links, don't expect me to duplicate them):
- docs/planning/2026-06-29-multi-gym-platform-roadmap.md — Phase-4 row: deliverable, "∥ 3 — the row-data merge exit needs 3's gym table", exit criteria, riskiest assumption #3 (no-FOUC/bundle).
- docs/adr/0012-host-brand-resolution.md — THE mechanism ADR: §3 dark-safe <style> tokens, §4 @gym/brand + boundary, Consequences "brand is CODE / per-gym is DATA", Forward-looking = the exact design Phase 4 implements (base module, ⊕-merge zod-validated, keyspace split) + the gym-column interface (defined by Phase 3), plus the rejected-alternatives list that pre-empts re-litigating. CITE — never edit (see COORDINATION).
- docs/superpowers/audits/2026-07-01-multitenant-branding-scale-audit.md — findings 2/8/11 that Phase 4 owns + the verified do-NOT-fix traps that bind this phase.
- docs/planning/2026-06-29-target-data-model-and-decisions.md — §4 gym-row columns incl. brand_module_id + token_overrides as Phase 3 defined them (the Phase-3→Phase-4 interface; read-only input).
- CONTEXT.md — locked Spanish vocabulary to extend, not fork (marca, contrato de marca, módulo de marca, host→inquilino→marca).
- docs/adr/0008 (presentation-only/never-authz hinge) + docs/adr/0011 (JIT raw-TS packaging; brand ✗→ data/domain cruiser edges) — settled, cite §.
- docs/prds/prd-multitenant-tracer.md — the forward note (~line 290) naming Phase 4's animation/copy/reduced-motion scope, and the ### Design principles placement pattern to mirror.
- Code anchors: packages/brand/src/registry.ts (BrandModule shape + two-entry record the base module extends); apps/admin/src/app/globals.css (the generalise target: ~22-var @theme contract, forge-* keyframes, global reduced-motion block @187) vs apps/client/src/app/globals.css (2 vars, NO keyframes, NO reduce block — a real gap this phase closes); apps/admin/src/app/layout.tsx (hardcodes #1/#2: title:18, ForgeToaster:6+60; + the shared x-brand→<style> pattern); hardcode #3's two lockup sites (inicio.tsx:6+63, recibo.tsx:4+70 — full paths in the audit); (auth)/login/_components/login-form.tsx (hardcode #4: the whole Forge sequence — the thing that becomes an animation module); packages/ui/src/motion.ts (existing reduced-motion JS seam); packages/ui/src/forge/skeleton.tsx:83 references 'forge-flash' by name across the package boundary — breaks if the client ever renders a Skeleton (note the file sits under the @gym/ui/forge/* namespace decisions (h)/(j) rule on).
Next 16 has breaking changes vs your training data — read node_modules/next/dist/docs/ before specifying any Next code. AGENTS.md carries the Husky-v9 caveat.

PIPELINE (run in order, quiz me at each checkpoint; exact skill names):
1. /grill-with-docs — lock Phase-4's terms into CONTEXT.md (ONLY your owned rows — see COORDINATION: marca / contrato / módulo de marca / the "A escala" paragraph, plus NEW rows for real domain terms it mints: módulo base, token overrides / personalización por gimnasio) and rule on the genuinely-OPEN decisions. Already LOCKED — cite, don't re-mint: SSR <style> injection + no :root in globals.css (ADR-0012 §3); no BrandSource/BrandModule<T>/React brand context (§1/§4 + rejected list) — and the resolver's sync→async/tenant evolution is Phase-3-owned, read-only for you (depend on the x-brand header, not the signature); presentation never authz (ADR-0008); brand=CODE / per-gym=DATA (Consequences); the base-module + ⊕-merge design itself (Forward-looking — you EXECUTE it); RED-admin admin-app enablement is pure seam data (§2 — today one host-map row, a gym_domain row after Phase 3's swap; what Phase 4 adds is the shell de-brand, audit finding 11); the audit do-NOT-fix list. OPEN — lock each:
   (a) token-override zod schema shape: closed key-enum of the ~25 contract vars vs open partial; light/dark overridable independently (the serialized block has BOTH :root/.light and .dark sections — a map keyed only by var name is ambiguous; the pinned column is an opaque jsonb, so this shape is YOURS to lock); CSS-value strictness (this schema IS the guard on dangerouslySetInnerHTML — free-string passthrough is an injection hole); where it lives (@gym/brand adding its own catalog zod dep is boundary-legal).
   (b) merge + serialization seam: modules stay pre-serialized strings vs structured objects through ONE serializer (Phase 2 rejected tokensToCss as one-caller; the second producer arrives NOW); per-request vs precomputed; how output feeds the layouts' brands[brandId].css index.
   (c) copy code/data split: módulo = tokens+logo+animation+copy vs gym-row DATA (brand_name/tagline exist in Phase 3's target schema) — enumerate the ACTUAL copy keys (metadata title/description, wordmark, negocio??'FORGE' fallbacks in cuenta.tsx:99 + plantilla-editor.tsx:39, toaster/receipt name) and the base module's neutral voice.
   (d) prefers-reduced-motion expression: per-app global block vs shared @gym/ui sheet vs per-module @media; what "respected" means for a login hero (skip vs final frame); how it's OBSERVED.
   (e) render-layer keyspace (audit finding 8's implementation half): BrandId grows to 'forge'|'red'|'base'; does DEFAULT_BRAND become 'base'? — this is the ONE fallback knob (the pinned header AND column contracts both defer unknown/absent values to DEFAULT_BRAND, whatever you set it to); the brand.test.ts census (line 13) + DEFAULT_BRAND assertions (16–19) updates are yours (deliberate tripwire).
   (f) exit-demo path given ∥ Phase 3: fixture override object through the REAL merge path vs seeded gym row vs sequencing the merge slice after 3's table lands (columns are pre-pinned — see COORDINATION; you never write the migration). NOTE: this decision determines whether the cross-initiative Blocked-by exists at all.
   (g) admin de-brand boundary: the 4 audit sites are settled scope; rule in/out the residue — app/icon.svg favicon, viewport themeColor Forge hexes (layout.tsx:29-30), negocio fallbacks, nav.ts:18 'forge:cameFromApp', forge-respaldo-* filename, forge-* class/keyframe NAMES (product naming vs brand leakage).
   (h) animation ownership: brand-bespoke vs product motion keyframes; where they live so Skeleton's 'forge-flash' works in apps/client.
   (i) RED-admin go-live: bias toward LOCAL-HOST-ONLY proof for the parallel window; live go-live is a post-Phase-3-swap HITL step — the human inserts the gym_domain row + provisions the host (HOST_TO_BRAND's 5 entries contain no RED-admin host, so Phase 3's seed never creates it) — NOT a second cross-initiative edge.
   (j) ForgeToaster / @gym/ui/forge/* namespace: brand-neutral rename/re-export vs module-provided component — decide narrowly, no repo-wide rename.
   ADR posture: do NOT mint an ADR that restates ADR-0012, and do NOT edit ADR-0012 while Phase 3 may still be amending it (Phase-3-owned). DEFAULT: record the grill's locked semantics of (a)/(c)/(d) in the PRD now, and land a short "Amended: (Phase 4)" pass on ADR-0012 once Phase 3's ADR-0012 edits have settled. Mint ADR-0015 ONLY if a lock CONTRADICTS ADR-0012's text rather than refines it. Do NOT run brainstorming — the mechanism is designed; this is locking enumerated semantics.
2. /to-prd — publish the Phase-4 PRD to the tracker + mirror to docs/prds/prd-brand-system.md (pinned filename). MUST embed the "### Design principles" section — the canonical keep-it-lean clause (copied from the skill at assembly time; see KEEP-IT-LEAN WIRING) immediately followed by the NAMED PRESENT-NEED EXCEPTIONS block below VERBATIM — at the end of ## Implementation Decisions. Carry a suggested S0..Sn decomposition, per-slice skills, and Out-of-Scope as guidance — the next stage owns the final cut.
3. /to-issues — thin vertical slices with a Blocked-by DAG. Initiative label: platform-phase4-brand-2026-07 (mint via gh label create; NEVER reuse or share a label — the Phase-3 session mints its own; no slice ever carries both). Schedule the base-module + row-override-merge slice LAST. IF grill (f) locked a demo needing Phase 3's real table, its body carries the cross-initiative edge in the parseable two-line section form:
   ## Blocked by
   #<N> — Phase 3 gym+gym_domain schema (cross-initiative)
   where #<N> is Phase 3's already-published gym-schema issue number (Phase 3 planning finished to-issues before you started — fetch it from the tracker or I supply it). Cite the real number; never a placeholder. If (f) locked the fixture path, there is NO cross-initiative edge and the slice is ordinary AFK. Everything else (token sets, logos, animations, copy, admin de-brand, reduced-motion) is code-only, truly parallel, AFK `ready-for-agent`. Exactly ONE terminal `hitl` slice (initiative label + hitl, NO ready-for-agent) bundling the human verification below.
4. /to-goal — emit docs/prompts/goal-platform-phase4-brand.md (pinned filename), K=3. Insert, phase-local into each slice's Gate 1 (Elegance) blockquote (AFTER "…minimum diff needed to satisfy the acceptance criteria." and BEFORE "Return `YES` or `NO`."), the canonical clause (copied from the skill) followed by the NAMED PRESENT-NEED EXCEPTIONS block VERBATIM. Do NOT edit the global gate-prompts.md — phase-local only. IF the cross-initiative Blocked-by exists, it will surface as an Orphan in Step 3 — expected, not a bug; the goal prompt marks that slice externally gated until the Phase-3 issue closes, basing its branch on main once the Phase-3 gym-schema slice has fast-forwarded there (prefer main; stack on the blocker's branch only if timing forces it). Quote the pinned x-brand/x-gym header contract (COORDINATION) in the goal prompt.
Then STOP; summarize ALL artifacts — the grill's doc commits (CONTEXT.md rows, any ADR-0015) plus the PRD, issues, and goal file. Do not execute slices.

ANTI-CREEP DELTAS (depth phase: build ONLY the enumerated structures — these are the traps you'd still get wrong):
- ZERO database work: no gym/gym_membership tables, no migration, no RLS, no resolver async swap, no HOST_TO_BRAND retirement, no proxy edits. The frontend track reads pinned column SHAPES; Phase 3 creates them.
- The complete in-phase brand census is base + forge + red. No third brand, no speculative presets, no theming DSL, no gym-facing override editor/authoring UI — Phase 4 ships the merge MECHANISM (schema + merge + render); overrides arrive as row data.
- No dynamic() per-brand imports, no per-gym env vars, no Edge/multi-region, no BYO-domain queue, no parametrizing es-MX/MXN/+52 — audit-verified traps, all parked; no payments, notification channels, or observability work (Phases 6/7).
- No repo-wide 'forge' rename crusade (forge-pressable/-hit/-scroll classes, @gym/ui/forge/* subpaths, storage keys) unless the grill explicitly rules a site INTO the de-brand checklist — product naming ≠ brand leakage.
- The brand ✗→ data/domain cruiser edge is FROZEN: the merge is a pure function over values; override data arrives as an ARGUMENT fetched by the app (apps MAY import @gym/data). zod goes in packages/brand/package.json via the catalog — no cruiser edit.
- Keep every shield green (boundary, guards, catalog, server-only; lint+typecheck+test per commit). No new apps/packages paths expected — if a slice adds one, tree-describing docs update in the FOLLOWING slice so docs.test.ts stays green.

ACCEPTANCE (observable, not asserted — beyond what the docs already say):
- The per-brand JS bundle delta is RE-RECORDED in the slice after animation modules land (Phase 2 recorded 0 B; bundling both brands' animations changes that number — riskiest assumption #3 stays falsifiable).
- A 'base'-module gym demonstrably renders from base + row-override data through the REAL merge path (the demo form the grill locked in (f)) — screenshot/DOM evidence, not a unit test alone.
- The zod schema provably REJECTS a hostile override payload (e.g. a CSS value containing `</style>`) in a test that exercises the exact path feeding dangerouslySetInnerHTML.
- prefers-reduced-motion verified on the CLIENT app (which today has no reduce block at all — the exit criterion currently FAILS there) for both login heroes, per the semantics locked in (d).
- A Skeleton rendered in apps/client animates (the cross-package forge-flash dependency repaired, observed in-browser).

HITL / HUMAN-ONLY (the terminal `hitl` slice; orchestrator must not attempt):
- I supply the RED design mock (Desktop RED-1.0-Design/index.html — NOT in the repo) as the visual fidelity reference.
- Visual brand-fidelity sign-off (RED ignition, logos, both brands' chrome vs the mock) — taste judgment.
- I approve the admin-shell behaviour CHANGE: de-branding title/toaster/lockups/login-animation visibly changes what the live Forge operator sees — unlike Phase 2, this is not behaviour-preserving.
- Live RED-admin go-live (post-Phase-3-swap): I insert the gym_domain row, provision the RED-admin hostname on Vercel, and run the live deploy-verify mirroring #16 (both apps × both brands by host, no FOUC in a real browser).
- OS-level prefers-reduced-motion check on a real device for both login heroes; and I decide the base module's neutral copy voice.
- I relay Phase 3's gym-schema issue number at the to-issues checkpoint (if the edge exists).

PER-SLICE SKILLS (the goal file wires these in): /keep-it-lean ALWAYS (with the depth framing below — it shields against additions BEYOND the named needs, it must NOT reject the base module / zod schema / merge the exit criteria require); superpowers:writing-plans (the roadmap mandates a TDD bite-sized plan per phase); superpowers:test-driven-development on the pure targets — zod override schema, ⊕-merge, registry census (textbook, like resolveBrandId was); typescript-advanced-types-RED for the zod token-override schema (≈25 CSS vars × independent light/dark — real mapped/template-literal territory); frontend-design + the taste≥7 model rule (fable-5/opus-4.8) on animation/logo/RED-admin chrome, with the repo's .agents/skills/vercel-react-best-practices + vercel-composition-patterns rule-packs as reference for rerender/bundle perf + component API; /verify (no-FOUC and reduced-motion are observable claims — drive both apps on forge/red hosts); superpowers:requesting-code-review (fable-5/opus-4.8); superpowers:verification-before-completion; superpowers:using-git-worktrees — this initiative executes in parallel with Phase 3, so ship each slice in its own worktree. Bulk mechanical sub-tasks (token-block restructuring) run on sonnet-5 (cheapest capable per CLAUDE.md; escalate if it misses. Codex/gpt-5.5 is NOT installed; do not use codex-implementation or `codex exec`), never the taste-bearing work — which stays on fable-5/opus-4.8. Do NOT invoke (cargo-cult for a zero-schema frontend track): improve-database-architecture, supabase-postgres-best-practices-RED, any mcp__supabase__* tool (any DB touch is Phase-3 scope leakage), sector-map, prototype, to-map/to-findings/to-health, improve-codebase-architecture, migrate-to-shoehorn.

COORDINATION (Phase 3 planning finished through to-issues before this session; the two EXECUTE in parallel. Both kickoffs cite this same contract):
- ADR numbers: Phase 3 = ADR-0013 (gym-scoped RLS mechanism) + ADR-0014 (custom SMTP) + the ADR-0006 amendment. You take ADR-0015 only if a grill lock CONTRADICTS ADR-0012's text (default: a later "Amended: (Phase 4)" pass on ADR-0012, once Phase 3's ADR-0012 edits have settled). Phase 3 authors/amends ADR-0012; you cite it.
- Labels: platform-phase3-rls-2026-07 (theirs) vs platform-phase4-brand-2026-07 (yours). Distinct by construction — each orchestrator re-fetches by initiative label every turn, so a shared/reused label lets one run sweep the other's slices live. prd/ready-for-agent/hitl stay shared cross-cutting labels.
- Pinned filenames: PRDs docs/prds/prd-tenant-rls.md (theirs) / prd-brand-system.md (yours); goals docs/prompts/goal-platform-phase3-rls.md / goal-platform-phase4-brand.md.
- CONTEXT.md term-set ownership, append-only-disjoint: Phase 3 owns inquilino, host→inquilino→marca, the Flagged-ambiguity paragraph, and its new tenancy rows; Phase 4 owns marca, contrato de marca, módulo de marca, the "A escala" paragraph, and its new brand rows. Phase 3 committed its tenancy rows first; you extend the file with your brand rows. Add rows only for your set, edit only rows you own.
- Execution seam ownership: Phase 4 OWNS packages/brand/src/brand-id.ts (keyspace's brand half: BrandId incl. 'base', the DEFAULT_BRAND value — grill (e)), registry.ts + the new base/ module + the override-merge, and BOTH apps' layout.tsx (token injection + admin de-brand; REBASE HOTSPOT — Phase 3 never edits layouts). Phase 3 OWNS resolve-brand-id.ts (sync→async swap), host-map.ts (it DELETES it at the gym_domain swap — Phase 4 never adds rows there; the RED-admin host becomes a gym_domain row, not a map entry), BOTH proxies (REBASE HOTSPOT — Phase 4 makes ZERO proxy edits; any need goes through a cross-initiative Blocked-by, never a parallel edit), and the minimal HOST_TO_BRAND scrub of ARCHITECTURE.md (Phase 4 touches ARCHITECTURE.md only for its own tree-describing additions). Historical records (docs/prds/prd-multitenant-tracer.md, docs/prompts/goal-multitenant-tracer.md, docs/runbooks/*) are ARCHIVAL — neither phase rewrites them. .dependency-cruiser.cjs brand-is-presentation-only is frozen for both.
- SPLIT files (owned by block, not whole): packages/brand/src/brand.test.ts + index.ts. Phase 3 owns removing the HOST_TO_BRAND import (test line 4), the HOST_TO_BRAND describe block (lines 35–46), and index.ts's HOST_TO_BRAND export (line 6); Phase 4 owns the "@gym/brand registry" describe block (census line 13, DEFAULT_BRAND assertions lines 16–19) and any new index.ts exports. The blocks are disjoint — pull-rebase resolves any race.
- Pinned header contract (what keeps the two tracks commutative — quote it in the goal prompt): the proxy stamps x-gym = the resolved tenant id/slug (NEW, Phase 3 adds it; presentation/UX only, NEVER authz — ADR-0008 hinge) and x-brand = a registry key (today stamped from the static host-map; = the gym row's brand_module_id once Phase 3's DB lookup lands), ALWAYS validated in the layout via Object.hasOwn with DEFAULT_BRAND fallback — DEFAULT_BRAND's value is YOUR grill (e) call, the ONE fallback knob. Phase 3 stamps; Phase 4's layouts read both and never re-resolve; neither phase breaks this without sequencing via a blocker.
- Gym-row column interface (Phase 3 DEFINED, Phase 4 CONSUMES; read-only input from Phase 3's published PRD/schema slice): Phase 3's gym migration creates exactly `brand_module_id text NOT NULL` (opaque registry key; NO FK/CHECK and NO pinned DB default — render-side validation + the default/fallback module id are YOUR grill decisions (e)) and `token_overrides jsonb NOT NULL DEFAULT '{}'::jsonb` (the jsonb VALUE shape is YOUR grill decision (a); Phase 3 treats both as opaque), and seeds forge/red rows with brand_module_id 'forge'/'red' + '{}' overrides. You never write a migration against gym.
- AT MOST ONE cross-initiative edge: your "base module + gym-row override merge" slice is Blocked-by Phase 3's gym+gym_domain schema slice IF (and only if) your grill decision (f) locks a demo needing the real table; if it locks the fixture-through-the-real-merge-path demo, there is NO edge. Schedule your merge slice LAST (Phase 3 puts its schema slice FIRST, so any blocking window is near zero). Issue-number handshake is trivial: Phase 3 published its issues before you started, so you cite its real gym-schema issue number directly — never a placeholder. Merge order to main: Phase-3 gym-schema before your merge slice; everything else in either stack is order-free. RED-admin go-live is NOT a second edge: its gym_domain row is a later human insert (your HITL slice, post-swap).

KEEP-IT-LEAN WIRING: invoke /keep-it-lean; its ONE canonical clause is the blockquote in its "Wiring it into the pipeline" section — COPY IT FROM THE SKILL at assembly time (single source; if any copy and the skill ever differ, the skill file wins). Do not paraphrase it, and do not add a "thin tracer / prefer duplication over structure" preamble (that was Phase-2 tracer framing; this is a depth phase whose acceptance criteria REQUIRE the structures named below). Assembly: to-prd embeds the canonical clause + the NAMED PRESENT-NEED EXCEPTIONS block below as the "### Design principles" section; to-goal carries the same pair phase-local into each slice's Gate 1 (Elegance) blockquote at the insertion point given in PIPELINE step 4. NEVER edit the global to-goal gate-prompts.md.

------------------------------------------------------------------------------------------------
VERBATIM — NAMED PRESENT-NEED EXCEPTIONS block (pasted immediately after the canonical clause in both locations):

**Named present-need exceptions (Phase 4 — these structures are REQUIRED by the acceptance criteria and PASS the gate; reject structure BEYOND them):** (1) the neutral `base` brand module — its consumer is the phase's own exit criterion (a gym with no dedicated code module renders from base + row data); (2) the zod token-override schema — it guards an existing dangerouslySetInnerHTML sink in BOTH apps' layouts, a real injection surface today; (3) the module-baseline ⊕ row-overrides merge/serializer — its legitimate second producer exists in-phase (ADR-0012 recorded that tokensToCss was inlined in Phase 2 precisely because it had one caller; that condition ends now); (4) animation modules — two concrete in-phase members (the Forge login sequence extracted from login-form.tsx; the extant RED ignition) plus the cross-package forge-flash Skeleton dependency to repair. Anything beyond this list — a third brand, brand presets, a theming DSL, an override editor or authoring surface, a config package — fails the gate.
------------------------------------------------------------------------------------------------
```

After paste: the session should confirm the tree (Phase 3's gym columns + gym-schema issue already exist), run the four skills in order pausing at each checkpoint, then stop with a summary of all artifacts. Execution happens later in a fresh session off the emitted goal file, in parallel with (and label-isolated from) the Phase-3 run.