# Phase 3 (tenant/RLS) → Phase 4 (brand system) — Launch Coordination

> Operator note. **Staggered planning, parallel execution:** Phase 3 planning runs first (at least through `to-issues`), then Phase 4 planning starts; the two goal files then execute in parallel. Both kickoff prompts cite this contract.

**No pre-step.** Phase 3 DEFINES the gym-row columns in its own grill/gym-schema slice — `brand_module_id text NOT NULL` (opaque key into @gym/brand's code-module registry; no FK/CHECK, no DB default — render-side validation + the default/fallback module id are Phase-4 grill calls; seeded 'forge'/'red') and `token_overrides jsonb NOT NULL DEFAULT '{}'::jsonb` (per-gym override data; the jsonb value shape is Phase 4's zod decision; seeded '{}'). Because Phase 4 planning starts *after* Phase 3 has published, Phase 4 simply consumes those columns from Phase 3's PRD/issues — nothing to pin ahead of time.

**Assignments (identical in both kickoffs).** ADRs: Phase 3 = ADR-0013 (gym-scoped RLS) + ADR-0014 (custom SMTP) + the ADR-0006 amendment; Phase 4 = ADR-0015, minted only if a lock contradicts ADR-0012 (default: a later "Amended: (Phase 4)" pass on ADR-0012). Phase 3 authors/amends ADR-0012; Phase 4 cites it. Labels: `platform-phase3-rls-2026-07` / `platform-phase4-brand-2026-07` — no slice ever carries both. Filenames: prd-tenant-rls.md + goal-platform-phase3-rls.md / prd-brand-system.md + goal-platform-phase4-brand.md.

**CONTEXT.md rule.** Append-only-disjoint term-set ownership: Phase 3 = inquilino, host→inquilino→marca, Flagged-ambiguity, new tenancy rows; Phase 4 = marca, contrato de marca, módulo de marca, "A escala", new brand rows. Phase 3 commits its rows first; Phase 4 extends the file afterward. Edit only owned rows.

**The one cross-initiative dependency (conditional, execution-only).** Phase 4's base-module + row-override-merge slice is Blocked-by Phase 3's gym+gym_domain schema slice IF Phase 4's grill (f) locks a demo needing the real table; fixture path → no edge. Phase 3 schedules that schema slice first; Phase 4 its merge slice last. Since Phase 3's issues are already published, Phase 4 cites the real gym-schema issue number directly (no placeholder, no mid-flight relay). Merge-to-main order: Phase-3 gym-schema before Phase-4's merge slice. RED-admin go-live is deliberately NOT a second edge (post-swap HITL; the human inserts its gym_domain row).

**Execution seam (parallel runs).** Phase 3 owns the resolution side — resolve-brand-id.ts (sync→async), host-map.ts (deletes it), both proxies, the ARCHITECTURE.md HOST_TO_BRAND scrub. Phase 4 owns the render side — brand-id.ts (keyspace + DEFAULT_BRAND), registry.ts + base module + zod merge, both layouts. Shared test/index files are split by block (Phase 3 = HOST_TO_BRAND lines; Phase 4 = registry-census lines). The `brand ✗→ data/domain` cruiser edge is frozen for both. Header contract: proxy stamps `x-gym` (tenant, never authz) + `x-brand` (registry key = the row's brand_module_id once the DB lookup lands), layout-validated via `Object.hasOwn` with DEFAULT_BRAND fallback.

**Launch order.** (1) Paste the Phase-3 kickoff; run its pipeline. (2) Once Phase 3 has published its issues (cleared `to-issues`), paste the Phase-4 kickoff; run its pipeline. (3) Review both artifact sets; run the two goal files in fresh execution sessions in parallel. (4) Fast-forward Phase 3's gym-schema slice to main before Phase 4's merge slice lands.
