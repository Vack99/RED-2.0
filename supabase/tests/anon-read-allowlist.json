{
  "$comment": [
    "The checked-in anon-read surface (#214, epic #203). Every table a replay of",
    "supabase/migrations/ shows to be readable by the `anon` role must appear here with a",
    "scope and a reason, or `pnpm test` fails — see tools/guards/anon-read-surface.test.ts.",
    "",
    "The obligation set is DERIVED from the migrations, never declared, so there is no flag",
    "to dodge with: adding `for select to anon using (true)` to a new table fails the build",
    "until that table is listed here. `scope` is derived too — `all-gyms` means the USING",
    "predicate is literally `true`, i.e. ONE anonymous call returns every tenant's rows.",
    "A scoped policy quietly widening back to `using (true)` therefore fails exactly like a",
    "new table would; that widening is the drift that actually happened (the 2026-07-05",
    "health audit certified 'anon reads limited to gym + gym_domain', and 15 more landed",
    "the next day, unnoticed for 27 days).",
    "",
    "The publishable key is build-inlined into every browser bundle by design (ADR-0008),",
    "so this file IS the platform's public read surface. Adding a line is a product",
    "decision, not a formality.",
    "",
    "`gym_domain` is deliberately ABSENT: #216 took it off this surface entirely. anon now",
    "resolves one known hostname through public.gym_id_por_host (SECURITY DEFINER) and holds",
    "neither policy nor grant on the table, so it cannot enumerate the customer census.",
    "",
    "So are room, schedule_template, class_session_coach, class_type_workblock and",
    "class_type_bring_item: #250 (20260807120000) dropped their anon policies because no anon",
    "reader ever existed — the 2026-07-06 grant copied a PRD table list, not the pages that",
    "shipped. A line here needs a READER, not a plausible future page; re-adding one of those",
    "five means shipping the reader in the same change."
  ],
  "anonReadable": {
    "gym": {
      "scope": "all-gyms",
      "reason": "The pre-auth host->brand lookup runs before any session exists (resolveTenant reads id/slug/brand_module_id). Narrowed at the COLUMN grant instead of the policy: anon holds the 9 brand-seam columns plus, since #256, legal_name (the aviso's razon social -- public-by-law content, deliberately unscoped by row, same flat surface as brand_name). authenticated (since #213) still holds only the 9 brand-seam columns -- legal_name, owner_user_id and created_at raise 42501 for that role."
    },
    "gym_contact": {
      "scope": "scoped",
      "reason": "The public contacto page renders the gym own address/phone/hours. Scoped per gym since #215: the policy is gym_id = (select public.gym_en_peticion()), which reads the x-gym-id request header createAnonClient(gymId) sets. No header (unmapped host) -> zero rows, never an error."
    },
    "coach": {
      "scope": "scoped",
      "reason": "The public nosotros/precios pages render the gym coaches (getCoachesPublicos). Scoped per gym since #215: the policy is gym_id = (select public.gym_en_peticion()), which reads the x-gym-id request header createAnonClient(gymId) sets. No header (unmapped host) -> zero rows, never an error."
    },
    "class_type": {
      "scope": "scoped",
      "reason": "The public nosotros page renders the gym class formats (getFormatosPublicos) and the home page names today's sessions (getHorarioHoyPublico). Scoped per gym since #215: the policy is gym_id = (select public.gym_en_peticion()), which reads the x-gym-id request header createAnonClient(gymId) sets. No header (unmapped host) -> zero rows, never an error."
    },
    "class_session": {
      "scope": "scoped",
      "reason": "The public schedule. Scoped per gym since #215: the policy is gym_id = (select public.gym_en_peticion()), which reads the x-gym-id request header createAnonClient(gymId) sets. No header (unmapped host) -> zero rows, never an error."
    },
    "paquetes": {
      "scope": "scoped",
      "reason": "The public precios page renders the gym plans. Scoped per gym since #215: the policy is gym_id = (select public.gym_en_peticion()), which reads the x-gym-id request header createAnonClient(gymId) sets. No header (unmapped host) -> zero rows, never an error."
    },
    "plan_feature": {
      "scope": "scoped",
      "reason": "Feature bullets under each plan on the public precios page. Scoped per gym since #215: the policy is gym_id = (select public.gym_en_peticion()), which reads the x-gym-id request header createAnonClient(gymId) sets. No header (unmapped host) -> zero rows, never an error."
    },
    "about_value": {
      "scope": "scoped",
      "reason": "Public nosotros page content. Scoped per gym since #215: the policy is gym_id = (select public.gym_en_peticion()), which reads the x-gym-id request header createAnonClient(gymId) sets. No header (unmapped host) -> zero rows, never an error."
    },
    "facility": {
      "scope": "scoped",
      "reason": "Public nosotros page content. Scoped per gym since #215: the policy is gym_id = (select public.gym_en_peticion()), which reads the x-gym-id request header createAnonClient(gymId) sets. No header (unmapped host) -> zero rows, never an error."
    },
    "stat": {
      "scope": "scoped",
      "reason": "Public nosotros page content. Scoped per gym since #215: the policy is gym_id = (select public.gym_en_peticion()), which reads the x-gym-id request header createAnonClient(gymId) sets. No header (unmapped host) -> zero rows, never an error."
    },
    "faq": {
      "scope": "scoped",
      "reason": "Public precios/contacto page content. Scoped per gym since #215: the policy is gym_id = (select public.gym_en_peticion()), which reads the x-gym-id request header createAnonClient(gymId) sets. No header (unmapped host) -> zero rows, never an error."
    },
    "gym_legal": {
      "scope": "scoped",
      "reason": "The client app's per-tenant aviso de privacidad (#256) renders domicilio + the ARCO contact channel -- public-by-law content (LFPDPPP art. 15), not a leak. Scoped per gym via gym_id = (select public.gym_en_peticion()), same as gym_contact; column-narrowed too (20260808140000 revokes the ambient default grant and re-grants only gym_id/domicilio/email_arco/area_datos_personales -- updated_at stays closed). No header (unmapped host) -> zero rows, never an error."
    }
  }
}
