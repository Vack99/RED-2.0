# Supabase — Forge (project ref: hjppxawglmukfvsgmcog)
#
# Copy to `.env.local` IN THIS DIRECTORY (apps/admin/.env.local, gitignored) and
# fill in the publishable key. Next loads .env from the app directory, NOT the
# repo root — a root-level .env.local is silently ignored. The
# publishable (anon) key is safe to expose to the browser. The single-operator
# auth user + the real runtime values are provisioned in issue #2 (HITL); you
# can read the key with the Supabase MCP `get_publishable_keys` or from the
# project's API settings.

NEXT_PUBLIC_SUPABASE_URL=https://hjppxawglmukfvsgmcog.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
