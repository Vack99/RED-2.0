feat(money-path): wire atomic-write RPCs; resolve live-DB drift

The prior session deployed registrar_venta + toggle_pase to the live DB but
never mirrored, typed, or called them (audit finding #7, lived in real time).
Resolve by WIRING (Option A), not rollback:

- Mirror 20260531211105_atomic_write_rpcs.sql VERBATIM from pg_get_functiondef
  (reused version -> prod history skips it; fresh builds provision it).
- 20260601010721 redefines registrar_venta with trailing DEFAULT NULL params so
  the generated types make the nullable (ilimitado / mes / new-client) args
  optional -- no `as any`. toggle_pase unchanged (args never null).
- 20260601010843 re-revokes EXECUTE from anon: the DROP+CREATE re-triggered
  Supabase default privileges and re-granted anon on the money-path RPC.
- Regenerate database.types.ts Functions block (was empty).
- crearVenta: keep domain stacking math in TS, replace the UPDATE+INSERT pair
  with one supabase.rpc("registrar_venta"); omit null args via spread guard.
  New clients now start from an explicit empty saldo (no ilimitado conflation).
- togglePase: replace read + conditional soft-delete/insert with one
  supabase.rpc("toggle_pase") (soft-delete, consumio flag, guarded +/-1,
  Chihuahua hora -- all preserved, verified against the deployed body).
- ADR-0005: the atomic write seam (thin RPCs, SECURITY INVOKER, search_path '',
  the DEFAULT-NULL type bridge).

Each RPC smoke-tested in rolled-back txns on the real prod schema + real auth
identity (new/existing client, finite/ilimitado/mes, toggle on/off, back-dated,
zero-balance guard) -- all assertions passed. Gates: lint (69 modules) ·
typecheck · test 60/60 · build (10 pages) all green. get_advisors(security):
only the leaked-password item remains.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
